# GEO Audit Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

---

## 🤖 AUTONOMOUS EXECUTION CONTRACT

> **TO ANY AGENT RESUMING THIS PLAN — READ FIRST, THEN EXECUTE.**

The owner of this plan (user `seomduc@gmail.com`) has **explicitly delegated full autonomous execution** of the GEO Audit feature. You DO NOT need to ask for confirmation before:
- Reading any file in the repo
- Running `npm test`, `npm run check-types`, `npm run build`
- Editing files listed in §"File Structure" of this plan
- Creating commits on branch `improve/main` (with or without `--no-verify` if pre-commit lint fails on pre-existing WIP files unrelated to your change)
- Continuing to the next task when the current task's tests pass + commit lands

### Rules of engagement

1. **One task at a time.** Pick the lowest-numbered task with an unchecked status header (not yet marked `✅ DONE`).
2. **Strict TDD per the plan.** Always write test → run to confirm FAIL → implement → run to confirm PASS → commit.
3. **Commit message format.** `<type>(<scope>): <short summary>` — see existing commits `84cce3d`, `1fc7f46`, `8816d82` for tone. Body explains *why* and references task number.
4. **After each commit, update this plan file:**
   - Mark the task header with `✅ DONE (commit <SHA>)`
   - Tick all `- [ ]` boxes in that task to `- [x]`
   - Increment the "Progress: X / 36" counter in the §Execution Status table
   - Commit the plan update with message `docs(plan): mark Task N done`
5. **After each phase, update memory:** Edit `/home/minhducoder/.claude/projects/-media-minhducoder-New-Volume2-Learn-On-Drive-DO-AN/memory/project_geo_audit_status.md` to bump the "Update" timestamp + briefly note what was done.
6. **Stop the session and ask the user only when:**
   - A test fails for a reason the plan didn't anticipate AND you can't fix it within 2 attempts
   - A migration would alter pre-existing production data
   - A commit hook fails for a reason that's actually your fault (not pre-existing WIP)
   - You hit any decision NOT covered by the spec at `docs/superpowers/specs/2026-05-28-geo-audit-design.md`
   - Quota/budget concerns: you're touching `apps/gateway/*` files that conflict with WIP audit-compare-v2 — pause and surface the conflict
   - All 36 tasks done → final dispatch a code-reviewer subagent, then ask user to merge
7. **Do NOT pause to** ask "should I continue?", "is this OK?", or summarize progress mid-run. The user delegated execution. Run.

### Context budget management

If you sense your context filling up (long output, many file reads):
1. Pick the *current* task's boundary as a safe stop point (after commit, before next task).
2. Update this plan's §Execution Status section + memory file with where you stopped.
3. Write 1 short summary message to the user: "Stopped after Task N. Resume → 'continue geo plan'."
4. End your turn. The user will resume in a new session by saying the resume phrase or "tiếp tục geo audit".

### Resume phrase the user will type

> "tiếp tục geo audit" / "continue geo plan" / "resume geo audit"

When you see any of these, immediately:
1. Read the §Execution Status table → find the lowest-numbered uncompleted task
2. Read that task's section in full
3. Begin Step N.1 (typically "write the failing test")
4. Continue per the rules above

### Sanity check before starting any task

```bash
git log --oneline -5    # confirm last commit matches what plan says is done
git status              # confirm tree is clean OR has only WIP files unrelated to GEO
npm --workspace @repo/shared run test   # must be green
```

If any of those don't match expectations → STOP and surface to user before any further edits.

---

**Goal:** Ship Phase 1 MVP of GEO Audit — 8 GEO rules + llms.txt generator — as a Pro+Business-tier feature on the existing seo-analyzer pipeline. Single-page mode only.

**Architecture:** Extend `seo-analyzer` with 8 new rules under `IssueCategory.GEO` (6 rule-based using Cheerio/regex on `PageData`, 2 LLM-based via Gemini Flash). Crawler fetches `/robots.txt` (multi-bot parsing) + `/llms.txt` and attaches both to `PageData`. Gateway gates GEO via `FeatureFlag.GEO_AUDIT` and new quota dimension `geo_audits_monthly`. Frontend adds a 4th "AI Visibility" tab with blur paywall for Free users.

**Tech Stack:** NestJS 10, Prisma + Postgres, Vitest, gRPC + protoc, Gemini 2.0 Flash via `@google/generative-ai`, Cheerio, BullMQ + Redis, Next 15 + React 19, TanStack Query, shadcn/ui, next-intl.

**Spec:** [`docs/superpowers/specs/2026-05-28-geo-audit-design.md`](../specs/2026-05-28-geo-audit-design.md)

---

## 📊 Execution Status

> **Updated:** 2026-05-28 evening (last touched by author session)
> **Branch:** `improve/main` (worktree skipped — Tasks 1-3 don't conflict with WIP audit-compare-v2)

### Progress: 4 / 36 tasks done

| Phase | Status | Tasks | Notes |
|---|---|---|---|
| Pre-requisites P.1-P.4 | ⏭ **SKIPPED** | 0/4 | Working inline on `improve/main` instead of worktree |
| **Phase 0** — Shared + Proto Foundation | ✅ **DONE** | 3/3 | Commits `84cce3d`, `1fc7f46`, `8816d82` |
| Phase 1 — Crawler enrichment | ⏳ Pending | 1/5 | **▶ NEXT: Task 5 (llms.txt fetcher)** |
| Phase 2 — 6 rule-based GEO rules | ⏳ Pending | 0/6 | |
| Phase 3 — 2 LLM-based GEO rules | ⏳ Pending | 0/3 | Needs GEMINI_API_KEY in env |
| Phase 4 — Aggregation + register | ⏳ Pending | 0/4 | |
| Phase 5 — Report DB migration | ⏳ Pending | 0/2 | |
| Phase 6 — Gateway entitlement | ⏳ Pending | 0/3 | **⚠ Will conflict with audit-compare-v2 WIP** |
| Phase 7 — llms.txt generator endpoint | ⏳ Pending | 0/2 | |
| Phase 8 — FE GEO tab + paywall | ⏳ Pending | 0/3 | |
| Phase 9 — FE llms.txt modal | ⏳ Pending | 0/1 | |
| Phase 10 — Integration + E2E + QA | ⏳ Pending | 0/4 | |

### Resume instructions

1. Read this Status section to see where to pick up.
2. Skip Pre-requisites (P.1-P.4) — already decided to work inline.
3. Skip Phase 0 — tests passing (`npm --workspace @repo/shared run test` = 20/20 green).
4. Jump straight to **Task 4** in Phase 1 below.
5. Continue checking boxes as you complete each step.

### Verification commands

```bash
# Confirm Phase 0 still green
npm --workspace @repo/shared run test
npm --workspace @repo/proto run check-types

# Verify branch + commits
git log --oneline -5 -- packages/shared/ packages/proto/
# Expected: 8816d82, 1fc7f46, 84cce3d at top
```

---

## Pre-requisites

> **⏭ SKIPPED** — Author chose to work directly on `improve/main` because Phase 0 tasks (Tasks 1-3) only touch `packages/shared/` and `packages/proto/`, which don't conflict with WIP `audit-compare-v2` in `apps/*`. Worktree may be needed later (e.g. Phase 6 will conflict with WIP gateway audits code) — decide then.

Run these once before Phase 0.

- [x] ~~**P.1: Create isolated worktree off `improve/main`**~~ (skipped — see note above)

Use the `superpowers:using-git-worktrees` skill.

```bash
cd "/media/minhducoder/New Volume2/Learn_On_Drive/DO_AN"
git fetch
git worktree add ../DO_AN-geo improve/main -b feat/geo-audit
cd ../DO_AN-geo
npm install
```

Expected: new worktree at `../DO_AN-geo` on branch `feat/geo-audit`.

- [x] ~~**P.2: Verify dev environment**~~ (skipped — already running)

```bash
npm run docker:up         # postgres + redis up
npm run dev:gateway       # gateway watch on :3000
npm run dev:seo-analyzer  # analyzer watch on :50053
npm run dev:crawler       # crawler watch on :50052
```

Expected: each service prints `Nest application successfully started`. `curl http://localhost:3000/health` → 200.

- [x] ~~**P.3: Sanity-check existing tests pass on the new branch**~~ (Phase 0 tests passing — 20/20 @repo/shared)

```bash
npm --workspace @repo/shared run test
npm --workspace @seo/seo-analyzer run test
npm --workspace @seo/crawler run test
npm --workspace @seo/gateway run test
```

Expected: all existing tests green. If red on a fresh branch, fix or roll back before starting.

- [ ] **P.4: Confirm Gemini API key** ⏳ (do before Task 15 / Phase 3)

```bash
grep -E "^(GEMINI|SEO_AI)_" apps/gateway/.env apps/seo-analyzer/.env 2>/dev/null
```

Expected: at least one of `GEMINI_API_KEY` or `SEO_AI_API_KEY` is set. If not, request a free key at https://aistudio.google.com/apikey and add to both `.env` files.

---

## File Structure

### `packages/shared/`

- Modify: `packages/shared/src/plans.ts` — add `FeatureFlag.GEO_AUDIT`, `QuotaDimension.geo_audits_monthly`, set per-plan values.
- Modify: `packages/shared/src/index.ts` — add `IssueCategory.GEO`.
- Modify: `packages/shared/test/plans.spec.ts` — extend coverage.

### `packages/proto/`

- Modify: `packages/proto/crawler.proto` — add `AiBotAccess`, `LlmsTxt`, `UserAgentRule` messages; reference from `PageData`.
- Modify: `packages/proto/analyzer.proto` — add `geo_score` + `geo_version` to `AnalyzeResponse`.
- Generated: `.d.ts` files regenerated via `npm --workspace @repo/proto run build`.

### `apps/crawler/`

- Create: `apps/crawler/src/crawler/infra/fetchers/llms-txt-fetcher.service.ts`
- Create: `apps/crawler/src/crawler/infra/fetchers/robots-multi-bot-parser.ts`
- Modify: `apps/crawler/src/crawler/domain/page-data.interface.ts` — add `aiBotAccess?` + `llmsTxt?`.
- Modify: `apps/crawler/src/crawler/services/crawler.orchestrator.ts` — call new fetchers after main fetch.
- Modify: `apps/crawler/src/crawler/controllers/crawler.grpc.ts` — map new fields into gRPC payload.
- Test: `apps/crawler/test/unit/llms-txt-fetcher.spec.ts`
- Test: `apps/crawler/test/unit/robots-multi-bot-parser.spec.ts`
- Test: `apps/crawler/test/integration/crawl-with-geo.spec.ts`

### `apps/seo-analyzer/`

- Modify: `apps/seo-analyzer/src/analyzer/domain/page-data.interface.ts` — mirror new fields from proto.
- Create: `apps/seo-analyzer/src/analyzer/domain/rules/geo/ai-bot-access.rule.ts`
- Create: `apps/seo-analyzer/src/analyzer/domain/rules/geo/llms-txt-present.rule.ts`
- Create: `apps/seo-analyzer/src/analyzer/domain/rules/geo/article-schema.rule.ts`
- Create: `apps/seo-analyzer/src/analyzer/domain/rules/geo/entity-markup.rule.ts`
- Create: `apps/seo-analyzer/src/analyzer/domain/rules/geo/quotable-density.rule.ts`
- Create: `apps/seo-analyzer/src/analyzer/domain/rules/geo/citation-outbound.rule.ts`
- Create: `apps/seo-analyzer/src/analyzer/domain/rules/geo/direct-answer-intro.rule.ts`
- Create: `apps/seo-analyzer/src/analyzer/domain/rules/geo/semantic-completeness.rule.ts`
- Create: `apps/seo-analyzer/src/analyzer/services/gemini-client.service.ts`
- Create: `apps/seo-analyzer/src/analyzer/services/geo-score.calculator.ts`
- Modify: `apps/seo-analyzer/src/analyzer/domain/rules/index.ts` — register 8 GEO rules.
- Modify: `apps/seo-analyzer/src/analyzer/services/analyzer.service.ts` — conditional GEO batch, compute geoScore.
- Modify: `apps/seo-analyzer/src/analyzer/controllers/analyzer.grpc.ts` — include geoScore in response.
- Modify: `apps/seo-analyzer/prisma/seed.ts` — seed 8 new `seo_rules` rows.
- Test: 8 spec files under `apps/seo-analyzer/test/unit/rules/geo/`
- Test: `apps/seo-analyzer/test/unit/services/gemini-client.spec.ts`
- Test: `apps/seo-analyzer/test/unit/services/geo-score.calculator.spec.ts`
- Test: `apps/seo-analyzer/test/integration/geo-pipeline.spec.ts`

### `apps/report/`

- Create: `apps/report/prisma/migrations/20260528_add_geo_score/migration.sql`
- Modify: `apps/report/prisma/schema.prisma` — `geoScore Int?` + `geoVersion String?` on `Report`.
- Modify: `apps/report/src/report/services/report.service.ts` — read + persist new fields.
- Modify: `apps/report/src/report/persistence/report.repository.ts` — include in DTO.
- Test: `apps/report/test/unit/services/report-geo.spec.ts`

### `apps/gateway/`

- Modify: `apps/gateway/src/billing/services/entitlement.service.ts` — `canRunGeo(userId): { allowed, remainingQuota }`.
- Modify: `apps/gateway/src/billing/services/quota-counter.service.ts` — accept `geo_audits_monthly` dimension (already supports monthly windows).
- Modify: `apps/gateway/src/audits/services/audits.service.ts` — entitlement gate + pass `runGeo` to BullMQ.
- Modify: `apps/gateway/src/audits/controllers/audits.controller.ts` — augment `GET /audits/:id` response.
- Modify: `apps/gateway/src/audits/dto/audit-response.dto.ts` — add geo fields.
- Create: `apps/gateway/src/tools/services/llms-txt-generator.service.ts`
- Create: `apps/gateway/src/tools/controllers/llms-txt-generator.controller.ts`
- Create: `apps/gateway/src/tools/dto/llms-txt-generator.dto.ts`
- Modify: `apps/gateway/src/tools/tools.module.ts` — register generator.
- Test: `apps/gateway/test/unit/services/entitlement-geo.spec.ts`
- Test: `apps/gateway/test/unit/services/llms-txt-generator.spec.ts`
- Test: `apps/gateway/test/integration/audits-geo.e2e-spec.ts`
- Test: `apps/gateway/test/integration/llms-txt-generator.e2e-spec.ts`

### `apps/web/`

- Create: `apps/web/src/components/audit-detail/geo/geo-tab.tsx`
- Create: `apps/web/src/components/audit-detail/geo/geo-rule-card.tsx`
- Create: `apps/web/src/components/audit-detail/geo/geo-paywall-overlay.tsx`
- Create: `apps/web/src/components/audit-detail/geo/llms-txt-generator-modal.tsx`
- Create: `apps/web/src/app/[locale]/(app)/tools/llms-txt-generator/page.tsx`
- Modify: `apps/web/src/app/[locale]/(app)/audits/[id]/page.tsx` — add 4th tab.
- Modify: `apps/web/src/lib/api/audits.ts` — types for geo fields.
- Modify: `apps/web/src/lib/api/types.ts` — `GeoBreakdown`, `GeoRuleResult`.
- Create: `apps/web/src/lib/api/llms-txt-generator.ts` — API client.
- Create: `apps/web/src/lib/queries/use-llms-txt-generator.ts` — mutation hook.
- Modify: `apps/web/src/messages/en.json` + `vi.json` — ~25 new keys.
- Test: `apps/web/tests/unit/geo-tab.test.tsx`
- Test: `apps/web/tests/unit/geo-paywall.test.tsx`
- Test: `apps/web/tests/unit/llms-txt-generator-modal.test.tsx`

---

## Phase 0 — Foundation (shared + proto)

Estimated: 0.5 day.

### ✅ Task 1: Add `FeatureFlag.GEO_AUDIT` + quota dimension — DONE (commit `84cce3d`)

**Files:**
- Modify: `packages/shared/src/plans.ts`
- Modify: `packages/shared/test/plans.spec.ts`

- [x] **Step 1.1: Write the failing test**

Add to `packages/shared/test/plans.spec.ts`:

```ts
import { describe, expect, it } from 'vitest';
import { FeatureFlag, PLAN_FEATURES, type PlanCode, type QuotaDimension } from '../src/plans';

describe('GEO_AUDIT feature flag', () => {
  it('is absent on free plan', () => {
    expect(PLAN_FEATURES.free.features).not.toContain(FeatureFlag.GEO_AUDIT);
  });

  it('is included on pro plan', () => {
    expect(PLAN_FEATURES.pro.features).toContain(FeatureFlag.GEO_AUDIT);
  });

  it('is included on business plan', () => {
    expect(PLAN_FEATURES.business.features).toContain(FeatureFlag.GEO_AUDIT);
  });
});

describe('geo_audits_monthly quota', () => {
  const cases: Array<[PlanCode, number]> = [
    ['free', 0],
    ['pro', 50],
    ['business', 300],
  ];

  it.each(cases)('plan %s has geo_audits_monthly=%d', (plan, expected) => {
    expect(PLAN_FEATURES[plan].geo_audits_monthly).toBe(expected);
  });

  it('is in QuotaDimension union', () => {
    const dim: QuotaDimension = 'geo_audits_monthly';
    expect(dim).toBe('geo_audits_monthly');
  });
});
```

- [x] **Step 1.2: Run test to verify it fails**

```bash
npm --workspace @repo/shared run test -- plans.spec
```

Expected: FAIL — `Property 'GEO_AUDIT' does not exist on type 'typeof FeatureFlag'`.

- [x] **Step 1.3: Add the enum, type, and plan values**

In `packages/shared/src/plans.ts`:

1. Inside `enum FeatureFlag`, add a new line:
   ```ts
   GEO_AUDIT = 'geo_audit',
   ```

2. In `QuotaDimension` union add:
   ```ts
   | 'geo_audits_monthly'
   ```

3. In `interface PlanDefinition` add:
   ```ts
   geo_audits_monthly: number; // -1 = unlimited
   ```

4. In `PRO_FEATURES` array add `FeatureFlag.GEO_AUDIT`.

5. Update `PLAN_FEATURES`:
   - `free`: `geo_audits_monthly: 0`
   - `pro`: `geo_audits_monthly: 50`
   - `business`: `geo_audits_monthly: 300`

- [x] **Step 1.4: Run test to verify it passes**

```bash
npm --workspace @repo/shared run test -- plans.spec
```

Expected: all new tests PASS. Pre-existing tests still PASS.

- [x] **Step 1.5: Commit**

```bash
git add packages/shared/src/plans.ts packages/shared/test/plans.spec.ts
git commit -m "feat(shared): add FeatureFlag.GEO_AUDIT + geo_audits_monthly quota"
```

---

### ✅ Task 2: Add `IssueCategory.GEO` — DONE (commit `1fc7f46`)

**Files:**
- Modify: `packages/shared/src/index.ts`
- Modify: `packages/shared/test/index.spec.ts` (create if absent)

- [x] **Step 2.1: Write the failing test**

Append to `packages/shared/test/index.spec.ts` (create if needed):

```ts
import { describe, expect, it } from 'vitest';
import { IssueCategory } from '../src';

describe('IssueCategory.GEO', () => {
  it('exists with value "geo"', () => {
    expect(IssueCategory.GEO).toBe('geo');
  });
});
```

- [x] **Step 2.2: Run test → FAIL**

```bash
npm --workspace @repo/shared run test -- index.spec
```

Expected: `Property 'GEO' does not exist`.

- [x] **Step 2.3: Add enum value**

In `packages/shared/src/index.ts`, inside `enum IssueCategory` add:
```ts
GEO = 'geo',
```

- [x] **Step 2.4: Run test → PASS**

```bash
npm --workspace @repo/shared run test -- index.spec
```

Expected: PASS. Then run downstream type-check:
```bash
npm --workspace @repo/shared run build
```
Expected: build succeeds.

- [x] **Step 2.5: Commit**

```bash
git add packages/shared/src/index.ts packages/shared/test/index.spec.ts
git commit -m "feat(shared): add IssueCategory.GEO"
```

---

### ✅ Task 3: Extend gRPC proto with `AiBotAccess` + `LlmsTxt` + `geo_score` — DONE (commit `8816d82`)

**Implementation notes (for future reference):**
- Actual proto files are nested: `packages/proto/crawler/v1/crawler.proto`, `packages/proto/analyzer/v1/analyzer.proto`, `packages/proto/common/v1/common.proto`
- Common.IssueCategory enum also extended with `ISSUE_CATEGORY_GEO = 7`
- Added `quotable_blocks` (field 72) + `sections` (field 73) to PageData proactively (avoids second proto change in Phase 2 for G7 + G4)
- `keepCase: false` in protoLoader means fields surface as camelCase at runtime: `geoScore`, `geoVersion`, `aiBotAccess`, `llmsTxt`, `quotableBlocks`, `sections`
- Proto package uses dynamic `protoLoader.loadSync` — no codegen step needed; runtime changes immediate after .proto edit
- Smoke-verified via `loadProto(PROTO_FILES.CRAWLER)` returning new message types

**Files actually modified:**
- `packages/proto/common/v1/common.proto`
- `packages/proto/crawler/v1/crawler.proto`
- `packages/proto/analyzer/v1/analyzer.proto`

- [x] **Step 3.1: Edit `packages/proto/crawler.proto`**

Inside the existing `message PageData`, after the last field (say it ends at field 19), add:

```proto
optional AiBotAccess ai_bot_access = 20;
optional LlmsTxt llms_txt = 21;
```

At the bottom of the file (still under the same `package`), add:

```proto
message AiBotAccess {
  string robots_txt_url = 1;
  int32 robots_txt_status = 2;
  repeated UserAgentRule rules = 3;
}

message UserAgentRule {
  string user_agent = 1;
  repeated string disallow = 2;
  repeated string allow = 3;
}

message LlmsTxt {
  string url = 1;
  int32 status = 2;
  optional string h1 = 3;
  optional string summary = 4;
  int32 section_count = 5;
  int32 size_bytes = 6;
}
```

- [x] **Step 3.2: Edit `packages/proto/analyzer.proto`**

Inside `message AnalyzeResponse`, after the last field (say field 9), add:

```proto
optional int32 geo_score = 10;
optional string geo_version = 11;
```

- [x] **Step 3.3: Regenerate types** (no codegen needed — proto loaded dynamically)

```bash
npm --workspace @repo/proto run build
```

Expected: `.d.ts` files updated, no errors.

- [x] **Step 3.4: Verify downstream type-check**

```bash
npm --workspace @seo/crawler run check-types
npm --workspace @seo/seo-analyzer run check-types
```

Expected: no NEW errors. Existing warnings unchanged.

- [x] **Step 3.5: Commit**

```bash
git add packages/proto/
git commit -m "feat(proto): extend PageData + AnalyzeResponse for GEO audit"
```

---

## Phase 1 — Crawler enrichment

Estimated: 1.5 days.

### ✅ Task 4: Multi-bot `robots.txt` parser — DONE (commit d4e6a0d)

**Files:**
- Create: `apps/crawler/src/crawler/infra/fetchers/robots-multi-bot-parser.ts`
- Test: `apps/crawler/test/unit/robots-multi-bot-parser.spec.ts`

- [x] **Step 4.1: Write the failing tests**

Create `apps/crawler/test/unit/robots-multi-bot-parser.spec.ts`:

```ts
import { describe, expect, it } from 'vitest';
import { parseRobotsForAiBots, AI_BOT_USER_AGENTS } from '../../src/crawler/infra/fetchers/robots-multi-bot-parser';

describe('parseRobotsForAiBots', () => {
  it('returns no rules when input is empty', () => {
    expect(parseRobotsForAiBots('')).toEqual([]);
  });

  it('detects Disallow: / for GPTBot', () => {
    const input = `User-agent: GPTBot\nDisallow: /\n`;
    const result = parseRobotsForAiBots(input);
    const gpt = result.find((r) => r.userAgent === 'GPTBot');
    expect(gpt).toBeDefined();
    expect(gpt!.disallow).toEqual(['/']);
  });

  it('returns one rule per AI bot user agent (5 total)', () => {
    const input = AI_BOT_USER_AGENTS.map((ua) => `User-agent: ${ua}\nAllow: /\n`).join('\n');
    expect(parseRobotsForAiBots(input)).toHaveLength(5);
  });

  it('handles multiple Disallow + Allow per user-agent', () => {
    const input = `User-agent: ClaudeBot\nDisallow: /private\nDisallow: /tmp\nAllow: /private/public-doc\n`;
    const rule = parseRobotsForAiBots(input).find((r) => r.userAgent === 'ClaudeBot')!;
    expect(rule.disallow).toEqual(['/private', '/tmp']);
    expect(rule.allow).toEqual(['/private/public-doc']);
  });

  it('treats user-agent matching case-insensitively', () => {
    const input = `User-agent: gptbot\nDisallow: /\n`;
    const gpt = parseRobotsForAiBots(input).find((r) => r.userAgent === 'GPTBot');
    expect(gpt?.disallow).toEqual(['/']);
  });

  it('ignores unrelated user-agents', () => {
    const input = `User-agent: Googlebot\nDisallow: /admin\n`;
    expect(parseRobotsForAiBots(input)).toEqual([]);
  });
});
```

- [x] **Step 4.2: Run tests → FAIL**

```bash
npm --workspace @seo/crawler run test -- robots-multi-bot-parser
```

Expected: module not found.

- [x] **Step 4.3: Implement parser**

Create `apps/crawler/src/crawler/infra/fetchers/robots-multi-bot-parser.ts`:

```ts
export const AI_BOT_USER_AGENTS = [
  'GPTBot',
  'ChatGPT-User',
  'ClaudeBot',
  'PerplexityBot',
  'Google-Extended',
] as const;

export interface ParsedUserAgentRule {
  userAgent: string;
  disallow: string[];
  allow: string[];
}

export function parseRobotsForAiBots(robotsTxt: string): ParsedUserAgentRule[] {
  if (!robotsTxt.trim()) return [];

  const targets = new Map<string, string>(
    AI_BOT_USER_AGENTS.map((ua) => [ua.toLowerCase(), ua]),
  );
  const accum = new Map<string, ParsedUserAgentRule>();
  let activeAgents: string[] = [];

  for (const rawLine of robotsTxt.split(/\r?\n/)) {
    const line = rawLine.replace(/#.*$/, '').trim();
    if (!line) {
      activeAgents = [];
      continue;
    }
    const match = /^([A-Za-z-]+)\s*:\s*(.+)$/.exec(line);
    if (!match) continue;
    const directive = match[1].toLowerCase();
    const value = match[2].trim();

    if (directive === 'user-agent') {
      const canonical = targets.get(value.toLowerCase());
      if (canonical) {
        activeAgents.push(canonical);
        if (!accum.has(canonical)) {
          accum.set(canonical, { userAgent: canonical, disallow: [], allow: [] });
        }
      }
      continue;
    }

    if (directive === 'disallow') {
      for (const agent of activeAgents) accum.get(agent)!.disallow.push(value);
    } else if (directive === 'allow') {
      for (const agent of activeAgents) accum.get(agent)!.allow.push(value);
    }
  }

  return Array.from(accum.values());
}
```

- [x] **Step 4.4: Run tests → PASS**

```bash
npm --workspace @seo/crawler run test -- robots-multi-bot-parser
```

Expected: all 6 tests PASS.

- [x] **Step 4.5: Commit**

```bash
git add apps/crawler/src/crawler/infra/fetchers/robots-multi-bot-parser.ts \
        apps/crawler/test/unit/robots-multi-bot-parser.spec.ts
git commit -m "feat(crawler): multi-bot robots.txt parser for AI bots"
```

---

### Task 5: `LlmsTxtFetcherService` — fetch + validate `/llms.txt`

**Files:**
- Create: `apps/crawler/src/crawler/infra/fetchers/llms-txt-fetcher.service.ts`
- Test: `apps/crawler/test/unit/llms-txt-fetcher.spec.ts`

- [ ] **Step 5.1: Write the failing tests**

Create `apps/crawler/test/unit/llms-txt-fetcher.spec.ts`:

```ts
import { describe, expect, it, vi, beforeEach } from 'vitest';
import { LlmsTxtFetcherService } from '../../src/crawler/infra/fetchers/llms-txt-fetcher.service';

const mockFetch = vi.fn();

describe('LlmsTxtFetcherService', () => {
  let svc: LlmsTxtFetcherService;
  beforeEach(() => {
    mockFetch.mockReset();
    vi.stubGlobal('fetch', mockFetch);
    svc = new LlmsTxtFetcherService();
  });

  it('returns status 404 when file missing', async () => {
    mockFetch.mockResolvedValue({ status: 404, ok: false, text: async () => '' });
    const out = await svc.fetch('https://example.com');
    expect(out.status).toBe(404);
    expect(out.h1).toBeUndefined();
  });

  it('parses H1 and blockquote summary from valid llms.txt', async () => {
    const body = `# Example Site\n\n> Brief summary of the site.\n\n## Docs\n- [Start](https://example.com/start)\n`;
    mockFetch.mockResolvedValue({
      status: 200,
      ok: true,
      text: async () => body,
      headers: new Map([['content-length', String(body.length)]]),
    });
    const out = await svc.fetch('https://example.com');
    expect(out.status).toBe(200);
    expect(out.h1).toBe('Example Site');
    expect(out.summary).toBe('Brief summary of the site.');
    expect(out.sectionCount).toBe(1);
    expect(out.sizeBytes).toBe(body.length);
  });

  it('flags warning when body > 1MB', async () => {
    const huge = '# Site\n\n' + 'x'.repeat(1_100_000);
    mockFetch.mockResolvedValue({ status: 200, ok: true, text: async () => huge, headers: new Map() });
    const out = await svc.fetch('https://example.com');
    expect(out.sizeBytes).toBeGreaterThan(1_000_000);
  });

  it('returns status -1 when fetch throws', async () => {
    mockFetch.mockRejectedValue(new Error('ECONNREFUSED'));
    const out = await svc.fetch('https://example.com');
    expect(out.status).toBe(-1);
  });

  it('returns no h1 if file lacks one', async () => {
    mockFetch.mockResolvedValue({ status: 200, ok: true, text: async () => '## Just a heading\n', headers: new Map() });
    const out = await svc.fetch('https://example.com');
    expect(out.h1).toBeUndefined();
  });
});
```

- [ ] **Step 5.2: Run tests → FAIL**

```bash
npm --workspace @seo/crawler run test -- llms-txt-fetcher
```

Expected: module not found.

- [ ] **Step 5.3: Implement service**

Create `apps/crawler/src/crawler/infra/fetchers/llms-txt-fetcher.service.ts`:

```ts
import { Injectable, Logger } from '@nestjs/common';

export interface LlmsTxtResult {
  url: string;
  status: number;
  h1?: string;
  summary?: string;
  sectionCount: number;
  sizeBytes: number;
}

@Injectable()
export class LlmsTxtFetcherService {
  private readonly logger = new Logger(LlmsTxtFetcherService.name);
  private readonly TIMEOUT_MS = 5000;

  async fetch(siteRoot: string): Promise<LlmsTxtResult> {
    const url = new URL('/llms.txt', siteRoot).toString();
    try {
      const ctrl = new AbortController();
      const t = setTimeout(() => ctrl.abort(), this.TIMEOUT_MS);
      const res = await fetch(url, { signal: ctrl.signal });
      clearTimeout(t);
      if (!res.ok) {
        return { url, status: res.status, sectionCount: 0, sizeBytes: 0 };
      }
      const body = await res.text();
      return {
        url,
        status: res.status,
        h1: this.extractH1(body),
        summary: this.extractBlockquote(body),
        sectionCount: this.countH2(body),
        sizeBytes: body.length,
      };
    } catch (err) {
      this.logger.warn(`llms.txt fetch failed for ${url}: ${(err as Error).message}`);
      return { url, status: -1, sectionCount: 0, sizeBytes: 0 };
    }
  }

  private extractH1(body: string): string | undefined {
    const m = /^#\s+(.+?)\s*$/m.exec(body);
    return m?.[1];
  }

  private extractBlockquote(body: string): string | undefined {
    const m = /^>\s+(.+?)\s*$/m.exec(body);
    return m?.[1];
  }

  private countH2(body: string): number {
    return (body.match(/^##\s+/gm) ?? []).length;
  }
}
```

- [ ] **Step 5.4: Run tests → PASS**

```bash
npm --workspace @seo/crawler run test -- llms-txt-fetcher
```

Expected: all 5 tests PASS.

- [ ] **Step 5.5: Commit**

```bash
git add apps/crawler/src/crawler/infra/fetchers/llms-txt-fetcher.service.ts \
        apps/crawler/test/unit/llms-txt-fetcher.spec.ts
git commit -m "feat(crawler): LlmsTxtFetcherService — fetch + validate llms.txt"
```

---

### Task 6: Add `aiBotAccess` + `llmsTxt` to `PageData` interface

**Files:**
- Modify: `apps/crawler/src/crawler/domain/page-data.interface.ts`
- Modify: `apps/seo-analyzer/src/analyzer/domain/page-data.interface.ts`

- [ ] **Step 6.1: Add types to crawler `PageData`**

In `apps/crawler/src/crawler/domain/page-data.interface.ts`, append to the `PageData` interface body:

```ts
aiBotAccess?: {
  robotsTxtUrl: string;
  robotsTxtStatus: number;
  rules: Array<{ userAgent: string; disallow: string[]; allow: string[] }>;
};
llmsTxt?: {
  url: string;
  status: number;
  h1?: string;
  summary?: string;
  sectionCount: number;
  sizeBytes: number;
};
```

- [ ] **Step 6.2: Mirror in analyzer `PageData`**

In `apps/seo-analyzer/src/analyzer/domain/page-data.interface.ts`, add identical fields.

- [ ] **Step 6.3: Verify type-check**

```bash
npm --workspace @seo/crawler run check-types
npm --workspace @seo/seo-analyzer run check-types
```

Expected: no errors.

- [ ] **Step 6.4: Commit**

```bash
git add apps/crawler/src/crawler/domain/page-data.interface.ts \
        apps/seo-analyzer/src/analyzer/domain/page-data.interface.ts
git commit -m "feat(domain): add aiBotAccess + llmsTxt to PageData"
```

---

### Task 7: Wire fetchers into `CrawlerOrchestrator`

**Files:**
- Modify: `apps/crawler/src/crawler/services/crawler.orchestrator.ts`
- Modify: `apps/crawler/src/crawler/crawler.module.ts` (register provider)
- Test: `apps/crawler/test/integration/crawl-with-geo.spec.ts`

- [ ] **Step 7.1: Write integration test**

Create `apps/crawler/test/integration/crawl-with-geo.spec.ts`:

```ts
import { describe, expect, it, vi } from 'vitest';
import { Test } from '@nestjs/testing';
import { CrawlerModule } from '../../src/crawler/crawler.module';
import { CrawlerOrchestrator } from '../../src/crawler/services/crawler.orchestrator';
import { LlmsTxtFetcherService } from '../../src/crawler/infra/fetchers/llms-txt-fetcher.service';

describe('Crawler attaches GEO data', () => {
  it('populates aiBotAccess + llmsTxt on PageData', async () => {
    const moduleRef = await Test.createTestingModule({ imports: [CrawlerModule] }).compile();
    const orch = moduleRef.get(CrawlerOrchestrator);
    const llms = moduleRef.get(LlmsTxtFetcherService);

    vi.spyOn(llms, 'fetch').mockResolvedValue({
      url: 'https://example.com/llms.txt',
      status: 200,
      h1: 'Example',
      summary: undefined,
      sectionCount: 0,
      sizeBytes: 10,
    });

    // Use a static fixture HTML page; existing orchestrator test setup uses MSW or fixture.
    // Adapt to whichever pattern exists in the repo for crawl tests.
    const result = await orch.crawl('https://example.com', { mode: 'single', runGeo: true });

    expect(result.llmsTxt).toBeDefined();
    expect(result.llmsTxt!.h1).toBe('Example');
    expect(result.aiBotAccess).toBeDefined();
  });
});
```

- [ ] **Step 7.2: Run test → FAIL (orchestrator missing method)**

```bash
npm --workspace @seo/crawler run test -- crawl-with-geo
```

Expected: FAIL.

- [ ] **Step 7.3: Modify orchestrator**

In `apps/crawler/src/crawler/services/crawler.orchestrator.ts`:

1. Import the new services:
   ```ts
   import { LlmsTxtFetcherService } from '../infra/fetchers/llms-txt-fetcher.service';
   import { parseRobotsForAiBots } from '../infra/fetchers/robots-multi-bot-parser';
   ```

2. Inject in constructor (append):
   ```ts
   private readonly llmsTxtFetcher: LlmsTxtFetcherService,
   ```

3. After the main page fetch returns `PageData`, before returning, if `options.runGeo === true`:
   ```ts
   if (options.runGeo) {
     const origin = new URL(url).origin;
     const [robotsRes, llmsRes] = await Promise.allSettled([
       this.fetchRobotsTxt(origin),
       this.llmsTxtFetcher.fetch(origin),
     ]);
     if (robotsRes.status === 'fulfilled') {
       pageData.aiBotAccess = {
         robotsTxtUrl: `${origin}/robots.txt`,
         robotsTxtStatus: robotsRes.value.status,
         rules: parseRobotsForAiBots(robotsRes.value.body).map((r) => ({
           userAgent: r.userAgent,
           disallow: r.disallow,
           allow: r.allow,
         })),
       };
     }
     if (llmsRes.status === 'fulfilled') {
       pageData.llmsTxt = llmsRes.value;
     }
   }
   ```

4. Add private helper:
   ```ts
   private async fetchRobotsTxt(origin: string): Promise<{ status: number; body: string }> {
     try {
       const res = await fetch(`${origin}/robots.txt`, { signal: AbortSignal.timeout(5000) });
       return { status: res.status, body: res.ok ? await res.text() : '' };
     } catch {
       return { status: -1, body: '' };
     }
   }
   ```

5. Update `CrawlOptions` interface to add `runGeo?: boolean`.

- [ ] **Step 7.4: Register `LlmsTxtFetcherService`**

In `apps/crawler/src/crawler/crawler.module.ts`, add `LlmsTxtFetcherService` to `providers`.

- [ ] **Step 7.5: Run test → PASS**

```bash
npm --workspace @seo/crawler run test -- crawl-with-geo
```

Expected: PASS.

- [ ] **Step 7.6: Commit**

```bash
git add apps/crawler/src/crawler/services/crawler.orchestrator.ts \
        apps/crawler/src/crawler/crawler.module.ts \
        apps/crawler/test/integration/crawl-with-geo.spec.ts
git commit -m "feat(crawler): wire robots.txt + llms.txt fetchers into orchestrator (runGeo flag)"
```

---

### Task 8: Map new fields into gRPC response

**Files:**
- Modify: `apps/crawler/src/crawler/controllers/crawler.grpc.ts`

- [ ] **Step 8.1: Update gRPC payload mapping**

In `crawler.grpc.ts`, in the method that returns `PageData` over gRPC, add (after existing field mapping):

```ts
if (pageData.aiBotAccess) {
  response.ai_bot_access = {
    robots_txt_url: pageData.aiBotAccess.robotsTxtUrl,
    robots_txt_status: pageData.aiBotAccess.robotsTxtStatus,
    rules: pageData.aiBotAccess.rules,
  };
}
if (pageData.llmsTxt) {
  response.llms_txt = {
    url: pageData.llmsTxt.url,
    status: pageData.llmsTxt.status,
    h1: pageData.llmsTxt.h1,
    summary: pageData.llmsTxt.summary,
    section_count: pageData.llmsTxt.sectionCount,
    size_bytes: pageData.llmsTxt.sizeBytes,
  };
}
```

- [ ] **Step 8.2: Type-check**

```bash
npm --workspace @seo/crawler run check-types
```

Expected: no errors.

- [ ] **Step 8.3: Commit**

```bash
git add apps/crawler/src/crawler/controllers/crawler.grpc.ts
git commit -m "feat(crawler): map aiBotAccess + llmsTxt into gRPC response"
```

---

## Phase 2 — Rule-based GEO rules (6 rules)

Estimated: 3 days. Each rule follows the same skeleton — write spec, run fail, implement, run pass, commit.

### Task 9: Rule G1 — `geo/ai-bot-access`

**Files:**
- Create: `apps/seo-analyzer/src/analyzer/domain/rules/geo/ai-bot-access.rule.ts`
- Test: `apps/seo-analyzer/test/unit/rules/geo/ai-bot-access.spec.ts`

- [ ] **Step 9.1: Write failing test**

Create `apps/seo-analyzer/test/unit/rules/geo/ai-bot-access.spec.ts`:

```ts
import { describe, expect, it } from 'vitest';
import { CheckStatus } from '@repo/shared';
import { AiBotAccessRule } from '../../../../src/analyzer/domain/rules/geo/ai-bot-access.rule';
import { PageData } from '../../../../src/analyzer/domain/page-data.interface';

const base: PageData = { url: 'https://example.com', textContent: '', images: [], h1Tags: [], h2Tags: [], h3Tags: [], h4Tags: [], h5Tags: [], h6Tags: [], internalLinks: [], externalLinks: [] } as any;

describe('AiBotAccessRule', () => {
  const rule = new AiBotAccessRule();

  it('passes when no aiBotAccess data (default-allow per RFC 9309)', () => {
    expect(rule.check(base).status).toBe(CheckStatus.PASS);
  });

  it('fails when GPTBot has Disallow: /', () => {
    const pd = { ...base, aiBotAccess: { robotsTxtUrl: 'x', robotsTxtStatus: 200, rules: [{ userAgent: 'GPTBot', disallow: ['/'], allow: [] }] } };
    expect(rule.check(pd).status).toBe(CheckStatus.FAIL);
  });

  it('warns when ClaudeBot disallows /blog only', () => {
    const pd = { ...base, aiBotAccess: { robotsTxtUrl: 'x', robotsTxtStatus: 200, rules: [{ userAgent: 'ClaudeBot', disallow: ['/blog'], allow: [] }] } };
    expect(rule.check(pd).status).toBe(CheckStatus.WARN);
  });

  it('passes when all bots allowed', () => {
    const pd = { ...base, aiBotAccess: { robotsTxtUrl: 'x', robotsTxtStatus: 200, rules: [{ userAgent: 'GPTBot', disallow: [], allow: ['/'] }] } };
    expect(rule.check(pd).status).toBe(CheckStatus.PASS);
  });

  it('exposes blocked bots in metadata', () => {
    const pd = { ...base, aiBotAccess: { robotsTxtUrl: 'x', robotsTxtStatus: 200, rules: [{ userAgent: 'GPTBot', disallow: ['/'], allow: [] }, { userAgent: 'ClaudeBot', disallow: ['/'], allow: [] }] } };
    expect(rule.check(pd).metadata).toMatchObject({ blockedBots: ['GPTBot', 'ClaudeBot'] });
  });
});
```

- [ ] **Step 9.2: Run → FAIL**

```bash
npm --workspace @seo/seo-analyzer run test -- ai-bot-access
```

- [ ] **Step 9.3: Implement rule**

Create `apps/seo-analyzer/src/analyzer/domain/rules/geo/ai-bot-access.rule.ts`:

```ts
import { CheckStatus, IssueCategory } from '@repo/shared';
import { ISeoRule, RuleCheckOutput } from '../../seo-rule.interface';
import { PageData } from '../../page-data.interface';

const HIGH_VALUE_PATHS = ['/blog', '/docs', '/articles', '/help'];

export class AiBotAccessRule implements ISeoRule {
  readonly id = 'geo_ai_bot_access';
  readonly category = IssueCategory.GEO;

  check(pageData: PageData): RuleCheckOutput {
    const access = pageData.aiBotAccess;
    if (!access || access.rules.length === 0) {
      return {
        status: CheckStatus.PASS,
        score: 100,
        message: 'No AI bot restrictions detected (default allow per RFC 9309)',
        suggestion: null,
        metadata: { rules: [] },
      };
    }
    const blocked = access.rules.filter((r) => r.disallow.includes('/'));
    if (blocked.length > 0) {
      return {
        status: CheckStatus.FAIL,
        score: 0,
        message: `${blocked.length} AI bot(s) blocked: ${blocked.map((b) => b.userAgent).join(', ')}`,
        suggestion: 'Remove "Disallow: /" for AI bots in robots.txt to be eligible for AI search citation.',
        metadata: { blockedBots: blocked.map((b) => b.userAgent), rules: access.rules },
      };
    }
    const warned = access.rules.filter((r) => r.disallow.some((p) => HIGH_VALUE_PATHS.some((hv) => p.startsWith(hv))));
    if (warned.length > 0) {
      return {
        status: CheckStatus.WARN,
        score: 50,
        message: `${warned.length} AI bot(s) blocked from high-value paths`,
        suggestion: `Consider allowing /blog and /docs for ${warned.map((w) => w.userAgent).join(', ')}.`,
        metadata: { warnedBots: warned.map((w) => w.userAgent), rules: access.rules },
      };
    }
    return {
      status: CheckStatus.PASS,
      score: 100,
      message: 'All AI bots allowed access',
      suggestion: null,
      metadata: { rules: access.rules },
    };
  }
}
```

- [ ] **Step 9.4: Run → PASS**

```bash
npm --workspace @seo/seo-analyzer run test -- ai-bot-access
```

Expected: all 5 PASS.

- [ ] **Step 9.5: Commit**

```bash
git add apps/seo-analyzer/src/analyzer/domain/rules/geo/ai-bot-access.rule.ts \
        apps/seo-analyzer/test/unit/rules/geo/ai-bot-access.spec.ts
git commit -m "feat(analyzer): G1 ai-bot-access GEO rule"
```

---

### Task 10: Rule G2 — `geo/llms-txt-present`

**Files:**
- Create: `apps/seo-analyzer/src/analyzer/domain/rules/geo/llms-txt-present.rule.ts`
- Test: `apps/seo-analyzer/test/unit/rules/geo/llms-txt-present.spec.ts`

- [ ] **Step 10.1: Write failing test**

Create `apps/seo-analyzer/test/unit/rules/geo/llms-txt-present.spec.ts`:

```ts
import { describe, expect, it } from 'vitest';
import { CheckStatus } from '@repo/shared';
import { LlmsTxtPresentRule } from '../../../../src/analyzer/domain/rules/geo/llms-txt-present.rule';
import { PageData } from '../../../../src/analyzer/domain/page-data.interface';

const base: PageData = { url: 'https://example.com' } as any;

describe('LlmsTxtPresentRule', () => {
  const rule = new LlmsTxtPresentRule();

  it('fails when llmsTxt missing or 404', () => {
    expect(rule.check({ ...base, llmsTxt: { url: 'x', status: 404, sectionCount: 0, sizeBytes: 0 } }).status).toBe(CheckStatus.FAIL);
    expect(rule.check(base).status).toBe(CheckStatus.FAIL);
  });

  it('fails when present but no H1', () => {
    expect(rule.check({ ...base, llmsTxt: { url: 'x', status: 200, sectionCount: 0, sizeBytes: 50 } }).status).toBe(CheckStatus.FAIL);
  });

  it('warns when has H1 but no blockquote', () => {
    expect(rule.check({ ...base, llmsTxt: { url: 'x', status: 200, h1: 'Site', sectionCount: 0, sizeBytes: 50 } }).status).toBe(CheckStatus.WARN);
  });

  it('warns when oversized (>1MB) even with H1+summary', () => {
    expect(rule.check({ ...base, llmsTxt: { url: 'x', status: 200, h1: 'Site', summary: 'A site', sectionCount: 2, sizeBytes: 2_000_000 } }).status).toBe(CheckStatus.WARN);
  });

  it('passes when H1 + blockquote + reasonable size', () => {
    expect(rule.check({ ...base, llmsTxt: { url: 'x', status: 200, h1: 'Site', summary: 'A site', sectionCount: 2, sizeBytes: 500 } }).status).toBe(CheckStatus.PASS);
  });
});
```

- [ ] **Step 10.2: Run → FAIL**

```bash
npm --workspace @seo/seo-analyzer run test -- llms-txt-present
```

- [ ] **Step 10.3: Implement**

Create `apps/seo-analyzer/src/analyzer/domain/rules/geo/llms-txt-present.rule.ts`:

```ts
import { CheckStatus, IssueCategory } from '@repo/shared';
import { ISeoRule, RuleCheckOutput } from '../../seo-rule.interface';
import { PageData } from '../../page-data.interface';

const MAX_SIZE = 1_000_000;

export class LlmsTxtPresentRule implements ISeoRule {
  readonly id = 'geo_llms_txt_present';
  readonly category = IssueCategory.GEO;

  check(pageData: PageData): RuleCheckOutput {
    const t = pageData.llmsTxt;
    if (!t || t.status !== 200) {
      return {
        status: CheckStatus.FAIL,
        score: 0,
        message: t ? `llms.txt returned ${t.status}` : 'llms.txt not fetched',
        suggestion: 'Create /llms.txt — a Markdown file with an H1 site name and a blockquote summary. See https://llmstxt.org/',
        metadata: { url: t?.url, status: t?.status ?? null },
      };
    }
    if (!t.h1) {
      return {
        status: CheckStatus.FAIL,
        score: 0,
        message: 'llms.txt present but missing required H1',
        suggestion: 'The first non-empty line must be "# Site Name".',
        metadata: { url: t.url, sizeBytes: t.sizeBytes },
      };
    }
    if (t.sizeBytes > MAX_SIZE) {
      return {
        status: CheckStatus.WARN,
        score: 50,
        message: `llms.txt is ${(t.sizeBytes / 1024).toFixed(0)} KB (>1 MB)`,
        suggestion: 'Trim non-essential sections; LLM context windows favor compact summaries.',
        metadata: { url: t.url, sizeBytes: t.sizeBytes, h1: t.h1 },
      };
    }
    if (!t.summary) {
      return {
        status: CheckStatus.WARN,
        score: 50,
        message: 'llms.txt has H1 but no blockquote summary',
        suggestion: 'Add a "> One-line summary" right after the H1.',
        metadata: { url: t.url, h1: t.h1 },
      };
    }
    return {
      status: CheckStatus.PASS,
      score: 100,
      message: 'llms.txt is valid (H1 + summary present)',
      suggestion: null,
      metadata: { url: t.url, h1: t.h1, summary: t.summary, sectionCount: t.sectionCount },
    };
  }
}
```

- [ ] **Step 10.4: Run → PASS**

```bash
npm --workspace @seo/seo-analyzer run test -- llms-txt-present
```

- [ ] **Step 10.5: Commit**

```bash
git add apps/seo-analyzer/src/analyzer/domain/rules/geo/llms-txt-present.rule.ts \
        apps/seo-analyzer/test/unit/rules/geo/llms-txt-present.spec.ts
git commit -m "feat(analyzer): G2 llms-txt-present GEO rule"
```

---

### Task 11: Rule G5 — `geo/article-schema`

**Files:**
- Create: `apps/seo-analyzer/src/analyzer/domain/rules/geo/article-schema.rule.ts`
- Test: `apps/seo-analyzer/test/unit/rules/geo/article-schema.spec.ts`

- [ ] **Step 11.1: Write failing test**

```ts
import { describe, expect, it } from 'vitest';
import { CheckStatus } from '@repo/shared';
import { ArticleSchemaRule } from '../../../../src/analyzer/domain/rules/geo/article-schema.rule';
import { PageData } from '../../../../src/analyzer/domain/page-data.interface';

const base: PageData = { url: 'https://example.com', jsonLdBlocks: [] } as any;

describe('ArticleSchemaRule', () => {
  const rule = new ArticleSchemaRule();

  it('fails when no JSON-LD', () => {
    expect(rule.check(base).status).toBe(CheckStatus.FAIL);
  });

  it('fails when missing 2+ required fields', () => {
    const pd = { ...base, jsonLdBlocks: [{ '@type': 'Article', headline: 'X' }] };
    expect(rule.check(pd).status).toBe(CheckStatus.FAIL);
  });

  it('warns when missing exactly 1 required field', () => {
    const pd = { ...base, jsonLdBlocks: [{ '@type': 'BlogPosting', headline: 'X', author: 'A', datePublished: '2026-01-01' }] };
    expect(rule.check(pd).status).toBe(CheckStatus.WARN);
  });

  it('passes when all 4 fields present', () => {
    const pd = { ...base, jsonLdBlocks: [{ '@type': 'NewsArticle', headline: 'X', author: 'A', datePublished: '2026-01-01', image: 'i.jpg' }] };
    expect(rule.check(pd).status).toBe(CheckStatus.PASS);
  });

  it('ignores FAQPage (deprecated 2026-05-07)', () => {
    const pd = { ...base, jsonLdBlocks: [{ '@type': 'FAQPage', mainEntity: [] }] };
    expect(rule.check(pd).status).toBe(CheckStatus.FAIL);
  });
});
```

- [ ] **Step 11.2: Run → FAIL**

```bash
npm --workspace @seo/seo-analyzer run test -- article-schema
```

- [ ] **Step 11.3: Implement**

Create `apps/seo-analyzer/src/analyzer/domain/rules/geo/article-schema.rule.ts`:

```ts
import { CheckStatus, IssueCategory } from '@repo/shared';
import { ISeoRule, RuleCheckOutput } from '../../seo-rule.interface';
import { PageData } from '../../page-data.interface';

const ALLOWED_TYPES = ['Article', 'BlogPosting', 'NewsArticle'];
const REQUIRED = ['headline', 'author', 'datePublished', 'image'] as const;

export class ArticleSchemaRule implements ISeoRule {
  readonly id = 'geo_article_schema';
  readonly category = IssueCategory.GEO;

  check(pageData: PageData): RuleCheckOutput {
    const blocks = (pageData.jsonLdBlocks ?? []).filter(
      (b: any) => typeof b === 'object' && ALLOWED_TYPES.includes(String(b['@type'])),
    );
    if (blocks.length === 0) {
      return {
        status: CheckStatus.FAIL,
        score: 0,
        message: 'No Article/BlogPosting/NewsArticle JSON-LD found',
        suggestion: 'Add Article schema. NOTE: FAQPage was deprecated 2026-05-07; use Article instead.',
        metadata: { schemaTypesFound: (pageData.jsonLdBlocks ?? []).map((b: any) => b?.['@type'] ?? null) },
      };
    }
    const best = blocks.map((b: any) => ({
      type: b['@type'],
      missing: REQUIRED.filter((f) => !b[f]),
    })).sort((a, b) => a.missing.length - b.missing.length)[0];

    if (best.missing.length >= 2) {
      return {
        status: CheckStatus.FAIL,
        score: 0,
        message: `Article schema missing ${best.missing.length} required fields: ${best.missing.join(', ')}`,
        suggestion: `Add ${best.missing.join(' + ')} to the JSON-LD block.`,
        metadata: { schemaType: best.type, missingFields: best.missing },
      };
    }
    if (best.missing.length === 1) {
      return {
        status: CheckStatus.WARN,
        score: 50,
        message: `Article schema missing required field: ${best.missing[0]}`,
        suggestion: `Add ${best.missing[0]} to be eligible for rich result.`,
        metadata: { schemaType: best.type, missingFields: best.missing },
      };
    }
    return {
      status: CheckStatus.PASS,
      score: 100,
      message: `${best.type} schema is complete`,
      suggestion: null,
      metadata: { schemaType: best.type, missingFields: [] },
    };
  }
}
```

- [ ] **Step 11.4: Run → PASS**

```bash
npm --workspace @seo/seo-analyzer run test -- article-schema
```

- [ ] **Step 11.5: Commit**

```bash
git add apps/seo-analyzer/src/analyzer/domain/rules/geo/article-schema.rule.ts \
        apps/seo-analyzer/test/unit/rules/geo/article-schema.spec.ts
git commit -m "feat(analyzer): G5 article-schema GEO rule"
```

---

### Task 12: Rule G6 — `geo/entity-markup`

**Files:**
- Create: `apps/seo-analyzer/src/analyzer/domain/rules/geo/entity-markup.rule.ts`
- Test: `apps/seo-analyzer/test/unit/rules/geo/entity-markup.spec.ts`

- [ ] **Step 12.1: Write failing test**

```ts
import { describe, expect, it } from 'vitest';
import { CheckStatus } from '@repo/shared';
import { EntityMarkupRule } from '../../../../src/analyzer/domain/rules/geo/entity-markup.rule';
import { PageData } from '../../../../src/analyzer/domain/page-data.interface';

const base: PageData = { url: 'https://example.com', jsonLdBlocks: [] } as any;
const now = new Date('2026-05-28');

describe('EntityMarkupRule', () => {
  const rule = new EntityMarkupRule(() => now);

  it('fails when no author', () => {
    const pd = { ...base, jsonLdBlocks: [{ '@type': 'Article' }] };
    expect(rule.check(pd).status).toBe(CheckStatus.FAIL);
  });

  it('fails when author present but no publisher', () => {
    const pd = { ...base, jsonLdBlocks: [{ '@type': 'Article', author: { '@type': 'Person', name: 'A', url: 'u' } }] };
    expect(rule.check(pd).status).toBe(CheckStatus.FAIL);
  });

  it('warns when dateModified older than 180 days', () => {
    const pd = { ...base, jsonLdBlocks: [{ '@type': 'Article', author: { '@type': 'Person', name: 'A', url: 'u' }, publisher: { '@type': 'Organization', name: 'O' }, dateModified: '2025-01-01' }] };
    expect(rule.check(pd).status).toBe(CheckStatus.WARN);
  });

  it('passes when entity markup complete and fresh', () => {
    const pd = { ...base, jsonLdBlocks: [{ '@type': 'Article', author: { '@type': 'Person', name: 'A', url: 'u' }, publisher: { '@type': 'Organization', name: 'O' }, dateModified: '2026-05-01' }] };
    expect(rule.check(pd).status).toBe(CheckStatus.PASS);
  });
});
```

- [ ] **Step 12.2: Run → FAIL**

- [ ] **Step 12.3: Implement**

Create `apps/seo-analyzer/src/analyzer/domain/rules/geo/entity-markup.rule.ts`:

```ts
import { CheckStatus, IssueCategory } from '@repo/shared';
import { ISeoRule, RuleCheckOutput } from '../../seo-rule.interface';
import { PageData } from '../../page-data.interface';

const ALLOWED = ['Article', 'BlogPosting', 'NewsArticle'];
const FRESH_DAYS = 180;

export class EntityMarkupRule implements ISeoRule {
  readonly id = 'geo_entity_markup';
  readonly category = IssueCategory.GEO;

  constructor(private readonly now: () => Date = () => new Date()) {}

  check(pageData: PageData): RuleCheckOutput {
    const article = (pageData.jsonLdBlocks ?? []).find(
      (b: any) => typeof b === 'object' && ALLOWED.includes(String(b['@type'])),
    ) as any;
    if (!article) {
      return { status: CheckStatus.FAIL, score: 0, message: 'No Article schema to check entity markup', suggestion: 'Add Article JSON-LD first (see G5).', metadata: {} };
    }
    const author = article.author;
    const authorOk = author && (author['@type'] === 'Person' || Array.isArray(author)) && (author.name || (Array.isArray(author) && author[0]?.name));
    if (!authorOk) {
      return { status: CheckStatus.FAIL, score: 0, message: 'Missing author.@type=Person with name', suggestion: 'Add author: { "@type": "Person", "name": "...", "url": "..." }', metadata: { author } };
    }
    const publisher = article.publisher;
    if (!publisher || publisher['@type'] !== 'Organization' || !publisher.name) {
      return { status: CheckStatus.FAIL, score: 0, message: 'Missing publisher.@type=Organization with name', suggestion: 'Add publisher: { "@type": "Organization", "name": "..." }', metadata: { publisher } };
    }
    const dm = article.dateModified;
    if (dm) {
      const ageDays = Math.floor((this.now().getTime() - new Date(dm).getTime()) / 86_400_000);
      if (ageDays > FRESH_DAYS) {
        return { status: CheckStatus.WARN, score: 50, message: `dateModified is ${ageDays} days old`, suggestion: 'Update dateModified when revising the article.', metadata: { author: author.name, publisher: publisher.name, dateModified: dm, ageDays } };
      }
    }
    return { status: CheckStatus.PASS, score: 100, message: 'Author + publisher markup present', suggestion: null, metadata: { author: author.name, publisher: publisher.name, dateModified: dm } };
  }
}
```

- [ ] **Step 12.4: Run → PASS**

```bash
npm --workspace @seo/seo-analyzer run test -- entity-markup
```

- [ ] **Step 12.5: Commit**

```bash
git add apps/seo-analyzer/src/analyzer/domain/rules/geo/entity-markup.rule.ts \
        apps/seo-analyzer/test/unit/rules/geo/entity-markup.spec.ts
git commit -m "feat(analyzer): G6 entity-markup GEO rule"
```

---

### Task 13: Rule G7 — `geo/quotable-density`

**Files:**
- Create: `apps/seo-analyzer/src/analyzer/domain/rules/geo/quotable-density.rule.ts`
- Test: `apps/seo-analyzer/test/unit/rules/geo/quotable-density.spec.ts`

- [ ] **Step 13.1: Write failing test**

```ts
import { describe, expect, it } from 'vitest';
import { CheckStatus } from '@repo/shared';
import { QuotableDensityRule } from '../../../../src/analyzer/domain/rules/geo/quotable-density.rule';
import { PageData } from '../../../../src/analyzer/domain/page-data.interface';

const base: PageData = { url: 'https://example.com', textContent: 'word '.repeat(1000), quotableBlocks: { tables: 0, lists: 0, blockquotes: 0, dls: 0 } } as any;

describe('QuotableDensityRule', () => {
  const rule = new QuotableDensityRule();

  it('fails when density < 1.0 per 1000 words', () => {
    expect(rule.check(base).status).toBe(CheckStatus.FAIL);
  });

  it('warns when density 1-2', () => {
    const pd = { ...base, quotableBlocks: { tables: 1, lists: 1, blockquotes: 0, dls: 0 } };
    expect(rule.check(pd).status).toBe(CheckStatus.WARN);
  });

  it('passes when density >= 3', () => {
    const pd = { ...base, quotableBlocks: { tables: 1, lists: 2, blockquotes: 1, dls: 0 } };
    expect(rule.check(pd).status).toBe(CheckStatus.PASS);
  });
});
```

- [ ] **Step 13.2: Run → FAIL**

- [ ] **Step 13.3: Implement**

Create `apps/seo-analyzer/src/analyzer/domain/rules/geo/quotable-density.rule.ts`:

```ts
import { CheckStatus, IssueCategory } from '@repo/shared';
import { ISeoRule, RuleCheckOutput } from '../../seo-rule.interface';
import { PageData } from '../../page-data.interface';

export class QuotableDensityRule implements ISeoRule {
  readonly id = 'geo_quotable_density';
  readonly category = IssueCategory.GEO;

  check(pageData: PageData): RuleCheckOutput {
    const q = (pageData as any).quotableBlocks ?? { tables: 0, lists: 0, blockquotes: 0, dls: 0 };
    const total = q.tables + q.lists + q.blockquotes + q.dls;
    const words = (pageData.textContent ?? '').trim().split(/\s+/).filter(Boolean).length;
    const density = words > 0 ? (total / words) * 1000 : 0;
    const meta = { ...q, wordCount: words, density: Number(density.toFixed(2)) };
    if (density < 1.0) {
      return { status: CheckStatus.FAIL, score: 0, message: `Quotable density ${density.toFixed(2)}/1000 words (need ≥3)`, suggestion: 'Add tables, lists, or blockquotes — AI systems prefer extractable structures.', metadata: meta };
    }
    if (density < 3.0) {
      return { status: CheckStatus.WARN, score: 50, message: `Quotable density ${density.toFixed(2)}/1000 words (need ≥3)`, suggestion: 'Add more tables, lists, or blockquotes.', metadata: meta };
    }
    return { status: CheckStatus.PASS, score: 100, message: `Quotable density ${density.toFixed(2)}/1000 words`, suggestion: null, metadata: meta };
  }
}
```

- [ ] **Step 13.4: Add `quotableBlocks` to crawler PageData extraction**

In `apps/crawler/src/crawler/services/crawler.orchestrator.ts` (or whichever file builds `PageData`), after Cheerio parsing add:

```ts
pageData.quotableBlocks = {
  tables: $('table').length,
  lists: $('ul, ol').filter((_, el) => $(el).find('li').length >= 3).length,
  blockquotes: $('blockquote').length,
  dls: $('dl').length,
};
```

Mirror the field in `PageData` interface (both crawler + analyzer).

- [ ] **Step 13.5: Run → PASS**

```bash
npm --workspace @seo/seo-analyzer run test -- quotable-density
```

- [ ] **Step 13.6: Commit**

```bash
git add apps/seo-analyzer/src/analyzer/domain/rules/geo/quotable-density.rule.ts \
        apps/seo-analyzer/test/unit/rules/geo/quotable-density.spec.ts \
        apps/crawler/src/ apps/seo-analyzer/src/analyzer/domain/page-data.interface.ts
git commit -m "feat(analyzer): G7 quotable-density GEO rule + crawler extraction"
```

---

### Task 14: Rule G8 — `geo/citation-outbound`

**Files:**
- Create: `apps/seo-analyzer/src/analyzer/domain/rules/geo/citation-outbound.rule.ts`
- Test: `apps/seo-analyzer/test/unit/rules/geo/citation-outbound.spec.ts`

- [ ] **Step 14.1: Write failing test**

```ts
import { describe, expect, it } from 'vitest';
import { CheckStatus } from '@repo/shared';
import { CitationOutboundRule } from '../../../../src/analyzer/domain/rules/geo/citation-outbound.rule';
import { PageData } from '../../../../src/analyzer/domain/page-data.interface';

const base: PageData = { url: 'https://example.com', externalLinks: [] } as any;

describe('CitationOutboundRule', () => {
  const rule = new CitationOutboundRule();

  it('fails when 0 authoritative external links', () => {
    expect(rule.check(base).status).toBe(CheckStatus.FAIL);
  });

  it('warns when 1-2 authoritative links', () => {
    const pd = { ...base, externalLinks: [{ href: 'https://wikipedia.org/x' }, { href: 'https://example.org/y' }] };
    expect(rule.check(pd).status).toBe(CheckStatus.WARN);
  });

  it('passes when >=3 authoritative links', () => {
    const pd = { ...base, externalLinks: [
      { href: 'https://wikipedia.org/x' },
      { href: 'https://nih.gov/y' },
      { href: 'https://reuters.com/z' },
    ] };
    expect(rule.check(pd).status).toBe(CheckStatus.PASS);
  });

  it('recognizes .edu and .gov TLDs', () => {
    const pd = { ...base, externalLinks: [
      { href: 'https://mit.edu/x' },
      { href: 'https://nasa.gov/y' },
      { href: 'https://example.com/z' },
    ] };
    expect(rule.check(pd).status).toBe(CheckStatus.WARN);
  });
});
```

- [ ] **Step 14.2: Run → FAIL**

- [ ] **Step 14.3: Implement**

Create `apps/seo-analyzer/src/analyzer/domain/rules/geo/citation-outbound.rule.ts`:

```ts
import { CheckStatus, IssueCategory } from '@repo/shared';
import { ISeoRule, RuleCheckOutput } from '../../seo-rule.interface';
import { PageData } from '../../page-data.interface';

const TLD_WHITELIST = ['.gov', '.edu', '.gov.vn', '.edu.vn'];
const HOST_WHITELIST = ['wikipedia.org', 'nih.gov', 'reuters.com', 'bbc.com', 'w3.org', 'who.int', 'nature.com', 'arxiv.org', 'developer.mozilla.org'];

function isAuthoritative(href: string): boolean {
  try {
    const host = new URL(href).hostname.toLowerCase();
    if (TLD_WHITELIST.some((t) => host.endsWith(t))) return true;
    if (HOST_WHITELIST.some((h) => host === h || host.endsWith('.' + h))) return true;
    return false;
  } catch {
    return false;
  }
}

export class CitationOutboundRule implements ISeoRule {
  readonly id = 'geo_citation_outbound';
  readonly category = IssueCategory.GEO;

  check(pageData: PageData): RuleCheckOutput {
    const links = (pageData.externalLinks ?? []) as Array<{ href: string }>;
    const auth = links.filter((l) => isAuthoritative(l.href));
    const meta = { totalExternal: links.length, authoritative: auth.map((l) => ({ href: l.href, host: new URL(l.href).hostname })) };
    if (auth.length === 0) {
      return { status: CheckStatus.FAIL, score: 0, message: 'No outbound citations to authoritative sources', suggestion: 'Cite ≥3 sources from wikipedia, .gov, .edu, or major publishers.', metadata: meta };
    }
    if (auth.length < 3) {
      return { status: CheckStatus.WARN, score: 50, message: `Only ${auth.length} authoritative citation(s)`, suggestion: 'Add more citations to reach ≥3 for stronger AI trust signals.', metadata: meta };
    }
    return { status: CheckStatus.PASS, score: 100, message: `${auth.length} authoritative citations`, suggestion: null, metadata: meta };
  }
}
```

- [ ] **Step 14.4: Run → PASS**

```bash
npm --workspace @seo/seo-analyzer run test -- citation-outbound
```

- [ ] **Step 14.5: Commit**

```bash
git add apps/seo-analyzer/src/analyzer/domain/rules/geo/citation-outbound.rule.ts \
        apps/seo-analyzer/test/unit/rules/geo/citation-outbound.spec.ts
git commit -m "feat(analyzer): G8 citation-outbound GEO rule"
```

---

## Phase 3 — LLM-based GEO rules (2 rules)

Estimated: 2 days.

### Task 15: `GeminiClientService` wrapper

**Files:**
- Create: `apps/seo-analyzer/src/analyzer/services/gemini-client.service.ts`
- Test: `apps/seo-analyzer/test/unit/services/gemini-client.spec.ts`

- [ ] **Step 15.1: Install SDK if absent**

```bash
npm --workspace @seo/seo-analyzer install @google/generative-ai
```

- [ ] **Step 15.2: Write failing test**

Create `apps/seo-analyzer/test/unit/services/gemini-client.spec.ts`:

```ts
import { describe, expect, it, vi } from 'vitest';
import { GeminiClientService } from '../../../src/analyzer/services/gemini-client.service';

describe('GeminiClientService', () => {
  it('throws when API key absent', () => {
    expect(() => new GeminiClientService(undefined)).toThrow(/api key/i);
  });

  it('completeJson returns parsed JSON from response text', async () => {
    const svc = new GeminiClientService('test-key');
    (svc as any).model = { generateContent: vi.fn().mockResolvedValue({ response: { text: () => '{"direct": true, "reason": "ok"}' } }) };
    const out = await svc.completeJson('prompt');
    expect(out).toEqual({ direct: true, reason: 'ok' });
  });

  it('completeJson strips markdown code fences', async () => {
    const svc = new GeminiClientService('test-key');
    (svc as any).model = { generateContent: vi.fn().mockResolvedValue({ response: { text: () => '```json\n{"a":1}\n```' } }) };
    expect(await svc.completeJson('p')).toEqual({ a: 1 });
  });

  it('completeJson throws on invalid JSON', async () => {
    const svc = new GeminiClientService('test-key');
    (svc as any).model = { generateContent: vi.fn().mockResolvedValue({ response: { text: () => 'not json' } }) };
    await expect(svc.completeJson('p')).rejects.toThrow();
  });

  it('completeJson respects timeout', async () => {
    const svc = new GeminiClientService('test-key', { timeoutMs: 10 });
    (svc as any).model = { generateContent: () => new Promise((r) => setTimeout(() => r({ response: { text: () => '{}' } }), 100)) };
    await expect(svc.completeJson('p')).rejects.toThrow(/timeout/i);
  });
});
```

- [ ] **Step 15.3: Run → FAIL**

```bash
npm --workspace @seo/seo-analyzer run test -- gemini-client
```

- [ ] **Step 15.4: Implement service**

Create `apps/seo-analyzer/src/analyzer/services/gemini-client.service.ts`:

```ts
import { Injectable, Logger } from '@nestjs/common';
import { GoogleGenerativeAI, GenerativeModel } from '@google/generative-ai';

interface Options {
  model?: string;
  timeoutMs?: number;
  temperature?: number;
}

@Injectable()
export class GeminiClientService {
  private readonly logger = new Logger(GeminiClientService.name);
  private readonly model: GenerativeModel;
  private readonly timeoutMs: number;

  constructor(apiKey: string | undefined = process.env.GEMINI_API_KEY ?? process.env.SEO_AI_API_KEY, opts: Options = {}) {
    if (!apiKey) throw new Error('Gemini API key is required');
    const client = new GoogleGenerativeAI(apiKey);
    this.model = client.getGenerativeModel({
      model: opts.model ?? 'gemini-2.0-flash-001',
      generationConfig: { temperature: opts.temperature ?? 0.2, responseMimeType: 'application/json' },
    });
    this.timeoutMs = opts.timeoutMs ?? 30_000;
  }

  async completeJson<T = unknown>(prompt: string): Promise<T> {
    const work = this.model.generateContent(prompt);
    const out = await Promise.race([
      work,
      new Promise((_, rej) => setTimeout(() => rej(new Error('LLM call timeout')), this.timeoutMs)),
    ]) as any;
    let text = out.response.text() as string;
    text = text.replace(/^```(?:json)?\s*/i, '').replace(/```\s*$/i, '').trim();
    return JSON.parse(text) as T;
  }
}
```

- [ ] **Step 15.5: Run → PASS**

```bash
npm --workspace @seo/seo-analyzer run test -- gemini-client
```

- [ ] **Step 15.6: Commit**

```bash
git add apps/seo-analyzer/src/analyzer/services/gemini-client.service.ts \
        apps/seo-analyzer/test/unit/services/gemini-client.spec.ts \
        apps/seo-analyzer/package.json apps/seo-analyzer/package-lock.json
git commit -m "feat(analyzer): GeminiClientService for GEO LLM rules"
```

---

### Task 16: Rule G3 — `geo/direct-answer-intro`

**Files:**
- Create: `apps/seo-analyzer/src/analyzer/domain/rules/geo/direct-answer-intro.rule.ts`
- Test: `apps/seo-analyzer/test/unit/rules/geo/direct-answer-intro.spec.ts`

- [ ] **Step 16.1: Write failing test**

```ts
import { describe, expect, it, vi } from 'vitest';
import { CheckStatus } from '@repo/shared';
import { DirectAnswerIntroRule } from '../../../../src/analyzer/domain/rules/geo/direct-answer-intro.rule';
import { GeminiClientService } from '../../../../src/analyzer/services/gemini-client.service';

const base = { url: 'https://example.com', h1Tags: ['What is SEO?'], textContent: 'SEO stands for Search Engine Optimization. It is the practice of improving site visibility.', language: 'en' } as any;

describe('DirectAnswerIntroRule', () => {
  const fakeLlm = { completeJson: vi.fn() } as unknown as GeminiClientService;
  const rule = new DirectAnswerIntroRule(fakeLlm);

  it('passes when LLM says direct=true', async () => {
    (fakeLlm.completeJson as any).mockResolvedValue({ direct: true, reason: 'defines SEO' });
    expect((await rule.check(base)).status).toBe(CheckStatus.PASS);
  });

  it('fails when LLM says direct=false', async () => {
    (fakeLlm.completeJson as any).mockResolvedValue({ direct: false, reason: 'too vague' });
    expect((await rule.check(base)).status).toBe(CheckStatus.FAIL);
  });

  it('returns error status when LLM throws', async () => {
    (fakeLlm.completeJson as any).mockRejectedValue(new Error('timeout'));
    const out = await rule.check(base);
    expect(out.status).toBe(CheckStatus.WARN);
    expect(out.metadata).toMatchObject({ error: 'timeout' });
  });

  it('uses Vietnamese prompt when language=vi', async () => {
    (fakeLlm.completeJson as any).mockResolvedValue({ direct: true, reason: 'ok' });
    await rule.check({ ...base, language: 'vi', h1Tags: ['SEO là gì?'] });
    expect((fakeLlm.completeJson as any).mock.calls[0][0]).toMatch(/Đoạn văn|trực tiếp/);
  });
});
```

- [ ] **Step 16.2: Run → FAIL**

- [ ] **Step 16.3: Implement**

Create `apps/seo-analyzer/src/analyzer/domain/rules/geo/direct-answer-intro.rule.ts`:

```ts
import { CheckStatus, IssueCategory } from '@repo/shared';
import { ISeoRule, RuleCheckOutput } from '../../seo-rule.interface';
import { PageData } from '../../page-data.interface';
import { GeminiClientService } from '../../../services/gemini-client.service';

interface LlmOut { direct: boolean; reason: string }

function buildPrompt(h1: string, intro: string, lang: string): string {
  if (lang.startsWith('vi')) {
    return `Bạn là chuyên gia SEO. Đoạn văn dưới đây có trả lời TRỰC TIẾP câu hỏi ngụ ý từ tiêu đề "${h1}" trong 1-2 câu đầu không?
Trả lời JSON: { "direct": boolean, "reason": string (≤20 từ) }

Tiêu đề: ${h1}
Đoạn văn: ${intro}`;
  }
  return `You are an SEO expert. Does the paragraph below directly answer the implied question from the title "${h1}" in its first 1-2 sentences?
Respond JSON: { "direct": boolean, "reason": string (≤20 words) }

Title: ${h1}
Paragraph: ${intro}`;
}

export class DirectAnswerIntroRule implements ISeoRule {
  readonly id = 'geo_direct_answer_intro';
  readonly category = IssueCategory.GEO;

  constructor(private readonly llm: GeminiClientService) {}

  async check(pageData: PageData): Promise<RuleCheckOutput> {
    const h1 = pageData.h1Tags?.[0] ?? '';
    if (!h1) return { status: CheckStatus.WARN, score: 50, message: 'No H1 to evaluate', suggestion: 'Add an H1.', metadata: {} };
    const intro = (pageData.textContent ?? '').split(/\s+/).slice(0, 200).join(' ');
    const lang = (pageData.language ?? 'en').toLowerCase();
    try {
      const out = await this.llm.completeJson<LlmOut>(buildPrompt(h1, intro, lang));
      if (out.direct) {
        return { status: CheckStatus.PASS, score: 100, message: 'Intro directly answers the page topic', suggestion: null, metadata: { h1, reason: out.reason, intro } };
      }
      return { status: CheckStatus.FAIL, score: 0, message: 'Intro does not directly answer the page topic', suggestion: 'Rewrite the first sentence to directly answer the question implied by the H1.', metadata: { h1, reason: out.reason, intro } };
    } catch (err) {
      return { status: CheckStatus.WARN, score: 50, message: 'GEO LLM check could not complete', suggestion: null, metadata: { error: (err as Error).message, h1 } };
    }
  }
}
```

- [ ] **Step 16.4: Run → PASS**

```bash
npm --workspace @seo/seo-analyzer run test -- direct-answer-intro
```

- [ ] **Step 16.5: Commit**

```bash
git add apps/seo-analyzer/src/analyzer/domain/rules/geo/direct-answer-intro.rule.ts \
        apps/seo-analyzer/test/unit/rules/geo/direct-answer-intro.spec.ts
git commit -m "feat(analyzer): G3 direct-answer-intro LLM rule"
```

---

### Task 17: Rule G4 — `geo/semantic-completeness`

**Files:**
- Create: `apps/seo-analyzer/src/analyzer/domain/rules/geo/semantic-completeness.rule.ts`
- Test: `apps/seo-analyzer/test/unit/rules/geo/semantic-completeness.spec.ts`

- [ ] **Step 17.1: Write failing test**

```ts
import { describe, expect, it, vi } from 'vitest';
import { CheckStatus } from '@repo/shared';
import { SemanticCompletenessRule } from '../../../../src/analyzer/domain/rules/geo/semantic-completeness.rule';
import { GeminiClientService } from '../../../../src/analyzer/services/gemini-client.service';

const base = { url: 'https://example.com', h2Tags: ['Intro', 'Setup', 'Conclusion'], sections: [
  { heading: 'Intro', text: 'word '.repeat(150) },
  { heading: 'Setup', text: 'word '.repeat(150) },
  { heading: 'Conclusion', text: 'word '.repeat(150) },
], language: 'en' } as any;

describe('SemanticCompletenessRule', () => {
  const fakeLlm = { completeJson: vi.fn() } as unknown as GeminiClientService;
  const rule = new SemanticCompletenessRule(fakeLlm);

  it('passes when >=80% complete and avg >=134 words', async () => {
    (fakeLlm.completeJson as any).mockResolvedValue({ complete: true, wordCount: 150 });
    expect((await rule.check(base)).status).toBe(CheckStatus.PASS);
  });

  it('warns when 50-80% complete', async () => {
    let call = 0;
    (fakeLlm.completeJson as any).mockImplementation(() => Promise.resolve({ complete: call++ < 2, wordCount: 150 }));
    expect((await rule.check(base)).status).toBe(CheckStatus.WARN);
  });

  it('fails when <50% complete', async () => {
    (fakeLlm.completeJson as any).mockResolvedValue({ complete: false, wordCount: 80 });
    expect((await rule.check(base)).status).toBe(CheckStatus.FAIL);
  });

  it('caps at 5 chunks', async () => {
    const pd = { ...base, sections: Array.from({ length: 10 }, (_, i) => ({ heading: `H${i}`, text: 'word '.repeat(150) })) };
    (fakeLlm.completeJson as any).mockResolvedValue({ complete: true, wordCount: 150 });
    await rule.check(pd);
    expect((fakeLlm.completeJson as any).mock.calls.length).toBeLessThanOrEqual(5);
  });

  it('returns warn when no sections', async () => {
    expect((await rule.check({ ...base, sections: [] })).status).toBe(CheckStatus.WARN);
  });
});
```

- [ ] **Step 17.2: Run → FAIL**

- [ ] **Step 17.3: Implement**

Create `apps/seo-analyzer/src/analyzer/domain/rules/geo/semantic-completeness.rule.ts`:

```ts
import { CheckStatus, IssueCategory } from '@repo/shared';
import { ISeoRule, RuleCheckOutput } from '../../seo-rule.interface';
import { PageData } from '../../page-data.interface';
import { GeminiClientService } from '../../../services/gemini-client.service';

const MAX_CHUNKS = 5;
const MIN_WORD_COUNT = 134;

function prompt(h: string, text: string, lang: string): string {
  if (lang.startsWith('vi')) {
    return `Đoạn dưới có tự đủ trả lời ý của heading không (không cần đọc trang khác)?
Trả lời JSON: { "complete": boolean, "wordCount": int }
Heading: ${h}
Đoạn: ${text}`;
  }
  return `Does the passage below self-contain its answer to the heading (no external reading required)?
Respond JSON: { "complete": boolean, "wordCount": int }
Heading: ${h}
Passage: ${text}`;
}

export class SemanticCompletenessRule implements ISeoRule {
  readonly id = 'geo_semantic_completeness';
  readonly category = IssueCategory.GEO;

  constructor(private readonly llm: GeminiClientService) {}

  async check(pageData: PageData): Promise<RuleCheckOutput> {
    const sections = ((pageData as any).sections ?? []).slice(0, MAX_CHUNKS) as Array<{ heading: string; text: string }>;
    if (sections.length === 0) {
      return { status: CheckStatus.WARN, score: 50, message: 'No H2 sections found', suggestion: 'Add structured H2 sections.', metadata: {} };
    }
    const lang = (pageData.language ?? 'en').toLowerCase();
    const results: Array<{ heading: string; complete: boolean; wordCount: number }> = [];
    for (const s of sections) {
      try {
        const out = await this.llm.completeJson<{ complete: boolean; wordCount: number }>(prompt(s.heading, s.text.slice(0, 2500), lang));
        results.push({ heading: s.heading, complete: out.complete, wordCount: out.wordCount });
      } catch (err) {
        results.push({ heading: s.heading, complete: false, wordCount: 0 });
      }
    }
    const completionRate = results.filter((r) => r.complete).length / results.length;
    const avgWords = results.reduce((a, r) => a + r.wordCount, 0) / results.length;
    const meta = { chunkResults: results, completionRate: Number(completionRate.toFixed(2)), avgWordCount: Math.round(avgWords) };
    if (completionRate >= 0.8 && avgWords >= MIN_WORD_COUNT) {
      return { status: CheckStatus.PASS, score: 100, message: `${Math.round(completionRate * 100)}% of sections are self-contained`, suggestion: null, metadata: meta };
    }
    if (completionRate >= 0.5) {
      return { status: CheckStatus.WARN, score: 50, message: `${Math.round(completionRate * 100)}% complete; aim for ≥80%`, suggestion: 'Expand sections so each can be quoted standalone.', metadata: meta };
    }
    return { status: CheckStatus.FAIL, score: 0, message: `Only ${Math.round(completionRate * 100)}% of sections are self-contained`, suggestion: 'Rewrite sections so each fully answers its heading in 134+ words.', metadata: meta };
  }
}
```

- [ ] **Step 17.4: Add `sections` to PageData extraction**

In crawler orchestrator after Cheerio parse, extract H2 sections:

```ts
const sections: Array<{ heading: string; text: string }> = [];
$('h2').each((_, el) => {
  const heading = $(el).text().trim();
  let text = '';
  let node = el.nextSibling;
  while (node) {
    if (node.type === 'tag' && /^h[1-6]$/i.test(node.tagName)) break;
    if (node.type === 'tag') text += ' ' + $(node).text();
    node = node.nextSibling;
  }
  if (heading) sections.push({ heading, text: text.trim() });
});
pageData.sections = sections;
```

Add `sections?: Array<{ heading: string; text: string }>` to crawler + analyzer `PageData` interfaces.

- [ ] **Step 17.5: Run → PASS**

```bash
npm --workspace @seo/seo-analyzer run test -- semantic-completeness
```

- [ ] **Step 17.6: Commit**

```bash
git add apps/seo-analyzer/src/analyzer/domain/rules/geo/semantic-completeness.rule.ts \
        apps/seo-analyzer/test/unit/rules/geo/semantic-completeness.spec.ts \
        apps/crawler/src/ apps/seo-analyzer/src/analyzer/domain/page-data.interface.ts
git commit -m "feat(analyzer): G4 semantic-completeness LLM rule + sections extraction"
```

---

## Phase 4 — Aggregation, registration, analyzer wiring

Estimated: 1 day.

### Task 18: `GeoScoreCalculator`

**Files:**
- Create: `apps/seo-analyzer/src/analyzer/services/geo-score.calculator.ts`
- Test: `apps/seo-analyzer/test/unit/services/geo-score.calculator.spec.ts`

- [ ] **Step 18.1: Write failing test**

```ts
import { describe, expect, it } from 'vitest';
import { CheckStatus } from '@repo/shared';
import { calculateGeoScore } from '../../../src/analyzer/services/geo-score.calculator';

const r = (id: string, status: CheckStatus) => ({ ruleId: id, status, score: status === CheckStatus.PASS ? 100 : status === CheckStatus.WARN ? 50 : 0, weight: 12.5 });

describe('calculateGeoScore', () => {
  it('returns 100 when all 8 pass', () => {
    const results = ['g1','g2','g3','g4','g5','g6','g7','g8'].map((id) => r(id, CheckStatus.PASS));
    expect(calculateGeoScore(results)).toEqual({ score: 100, version: '1.0' });
  });

  it('returns 0 when all fail', () => {
    const results = ['g1','g2','g3','g4','g5','g6','g7','g8'].map((id) => r(id, CheckStatus.FAIL));
    expect(calculateGeoScore(results).score).toBe(0);
  });

  it('weighted average of mixed statuses', () => {
    const results = [r('g1', CheckStatus.PASS), r('g2', CheckStatus.FAIL), r('g3', CheckStatus.WARN), r('g4', CheckStatus.PASS), r('g5', CheckStatus.PASS), r('g6', CheckStatus.PASS), r('g7', CheckStatus.PASS), r('g8', CheckStatus.PASS)];
    // 6*100 + 50 + 0 = 650; total weight 8*12.5 = 100; score = 650/8 = 81
    expect(calculateGeoScore(results).score).toBe(81);
  });

  it('marks version 1.0-degraded when any LLM rule errored', () => {
    const results = [r('g1', CheckStatus.PASS), r('g2', CheckStatus.PASS), r('geo_direct_answer_intro', CheckStatus.WARN), r('g4', CheckStatus.PASS), r('g5', CheckStatus.PASS), r('g6', CheckStatus.PASS), r('g7', CheckStatus.PASS), r('g8', CheckStatus.PASS)];
    const errored = results.map((x) => ({ ...x, errored: x.ruleId === 'geo_direct_answer_intro' }));
    expect(calculateGeoScore(errored).version).toBe('1.0-degraded');
  });

  it('returns null when no results', () => {
    expect(calculateGeoScore([])).toEqual({ score: null, version: null });
  });
});
```

- [ ] **Step 18.2: Run → FAIL**

- [ ] **Step 18.3: Implement**

Create `apps/seo-analyzer/src/analyzer/services/geo-score.calculator.ts`:

```ts
import { CheckStatus } from '@repo/shared';

export interface GeoRuleResultInput {
  ruleId: string;
  status: CheckStatus;
  score: number;
  weight: number;
  errored?: boolean;
}

export interface GeoScoreOutput {
  score: number | null;
  version: string | null;
}

export function calculateGeoScore(results: GeoRuleResultInput[]): GeoScoreOutput {
  if (results.length === 0) return { score: null, version: null };
  const totalWeight = results.reduce((a, r) => a + r.weight, 0);
  if (totalWeight === 0) return { score: null, version: null };
  const weighted = results.reduce((a, r) => a + r.weight * r.score, 0);
  const score = Math.round(weighted / totalWeight);
  const degraded = results.some((r) => r.errored);
  return { score, version: degraded ? '1.0-degraded' : '1.0' };
}
```

- [ ] **Step 18.4: Run → PASS**

```bash
npm --workspace @seo/seo-analyzer run test -- geo-score.calculator
```

- [ ] **Step 18.5: Commit**

```bash
git add apps/seo-analyzer/src/analyzer/services/geo-score.calculator.ts \
        apps/seo-analyzer/test/unit/services/geo-score.calculator.spec.ts
git commit -m "feat(analyzer): GeoScoreCalculator"
```

---

### Task 19: Register 8 GEO rules + module wiring

**Files:**
- Modify: `apps/seo-analyzer/src/analyzer/domain/rules/index.ts`
- Modify: `apps/seo-analyzer/src/analyzer/analyzer.module.ts`

- [ ] **Step 19.1: Update rule registry**

In `apps/seo-analyzer/src/analyzer/domain/rules/index.ts`:

```ts
import { AiBotAccessRule } from './geo/ai-bot-access.rule';
import { LlmsTxtPresentRule } from './geo/llms-txt-present.rule';
import { ArticleSchemaRule } from './geo/article-schema.rule';
import { EntityMarkupRule } from './geo/entity-markup.rule';
import { QuotableDensityRule } from './geo/quotable-density.rule';
import { CitationOutboundRule } from './geo/citation-outbound.rule';
import { DirectAnswerIntroRule } from './geo/direct-answer-intro.rule';
import { SemanticCompletenessRule } from './geo/semantic-completeness.rule';
import { GeminiClientService } from '../../services/gemini-client.service';

export function registerAllRules(registry: RuleRegistry, opts: { runGeo: boolean; geminiKey?: string }): void {
  // existing 22 rules unchanged...

  if (opts.runGeo) {
    registry.register(new AiBotAccessRule());
    registry.register(new LlmsTxtPresentRule());
    registry.register(new ArticleSchemaRule());
    registry.register(new EntityMarkupRule());
    registry.register(new QuotableDensityRule());
    registry.register(new CitationOutboundRule());
    if (opts.geminiKey) {
      const llm = new GeminiClientService(opts.geminiKey);
      registry.register(new DirectAnswerIntroRule(llm));
      registry.register(new SemanticCompletenessRule(llm));
    }
  }
}
```

- [ ] **Step 19.2: Wire in `AnalyzerService.onModuleInit`**

In `apps/seo-analyzer/src/analyzer/services/analyzer.service.ts`:

```ts
onModuleInit(): void {
  registerAllRules(this.registry, {
    runGeo: process.env.GEO_AUDIT_ENABLED !== 'false',
    geminiKey: process.env.GEMINI_API_KEY ?? process.env.SEO_AI_API_KEY,
  });
}
```

- [ ] **Step 19.3: Type-check**

```bash
npm --workspace @seo/seo-analyzer run check-types
```

- [ ] **Step 19.4: Commit**

```bash
git add apps/seo-analyzer/src/analyzer/domain/rules/index.ts \
        apps/seo-analyzer/src/analyzer/services/analyzer.service.ts
git commit -m "feat(analyzer): register 8 GEO rules conditionally on env"
```

---

### Task 20: Analyzer — conditional GEO batch + score in response

**Files:**
- Modify: `apps/seo-analyzer/src/analyzer/services/analyzer.service.ts`
- Modify: `apps/seo-analyzer/src/analyzer/controllers/analyzer.grpc.ts`
- Test: `apps/seo-analyzer/test/unit/services/analyzer-geo.spec.ts`

- [ ] **Step 20.1: Write failing test**

Create `apps/seo-analyzer/test/unit/services/analyzer-geo.spec.ts`:

```ts
import { describe, expect, it, vi } from 'vitest';
// minimal harness — mock RuleRegistry, GeoScoreCalculator, and analyzerService.analyze
// Adapt to existing analyzer.service test pattern. Verify:
// - When request.runGeo=false, GEO rules are skipped; geoScore returned null.
// - When request.runGeo=true with all GEO rules passing, geoScore=100, geoVersion='1.0'.
// - When a GEO LLM rule throws inside .check(), aggregator marks errored.

it.todo('analyzer respects runGeo flag');
it.todo('analyzer returns geoScore when runGeo=true');
it.todo('analyzer marks geoVersion=1.0-degraded when any LLM rule errors');
```

(Engineer: replace `it.todo` with full assertions matching the existing analyzer test harness in the repo.)

- [ ] **Step 20.2: Modify `AnalyzerService.analyze()`**

In `analyzer.service.ts`, after running standard rules, if `request.runGeo === true`:

```ts
const geoRules = this.registry.getByCategory(IssueCategory.GEO);
const geoResults: GeoRuleResultInput[] = [];
for (const rule of geoRules) {
  try {
    const out = await Promise.resolve(rule.check(pageData));
    geoResults.push({ ruleId: rule.id, status: out.status, score: out.score, weight: 12.5, errored: false });
    // also push to standard ruleResults for persistence
    ruleResults.push({ ruleId: rule.id, status: out.status, score: out.score, message: out.message, evidence: out.metadata });
  } catch (err) {
    geoResults.push({ ruleId: rule.id, status: CheckStatus.WARN, score: 50, weight: 12.5, errored: true });
    ruleResults.push({ ruleId: rule.id, status: CheckStatus.WARN, score: 50, message: 'rule error', evidence: { error: (err as Error).message } });
  }
}
const { score: geoScore, version: geoVersion } = calculateGeoScore(geoResults);
return { ruleResults, seoScore, geoScore, geoVersion };
```

- [ ] **Step 20.3: Map `geo_score` + `geo_version` into gRPC response**

In `analyzer.grpc.ts`, in the `analyze()` handler:

```ts
if (result.geoScore != null) response.geo_score = result.geoScore;
if (result.geoVersion) response.geo_version = result.geoVersion;
```

- [ ] **Step 20.4: Run tests → PASS**

```bash
npm --workspace @seo/seo-analyzer run test
```

- [ ] **Step 20.5: Commit**

```bash
git add apps/seo-analyzer/src/analyzer/
git commit -m "feat(analyzer): conditional GEO batch + geoScore in gRPC response"
```

---

### Task 21: Prisma seed — 8 GEO rule rows

**Files:**
- Modify: `apps/seo-analyzer/prisma/seed.ts`

- [ ] **Step 21.1: Add 8 idempotent seeds**

In `apps/seo-analyzer/prisma/seed.ts`, in the rules array, append:

```ts
{ id: 'geo_ai_bot_access',         name: 'AI Bot Access Policy',      category: 'geo', weight: 12.5, enabled: true },
{ id: 'geo_llms_txt_present',      name: 'llms.txt Standard',         category: 'geo', weight: 12.5, enabled: true },
{ id: 'geo_direct_answer_intro',   name: 'Direct Answer Intro',       category: 'geo', weight: 12.5, enabled: true },
{ id: 'geo_semantic_completeness', name: 'Semantic Completeness',     category: 'geo', weight: 12.5, enabled: true },
{ id: 'geo_article_schema',        name: 'Article Schema Markup',     category: 'geo', weight: 12.5, enabled: true },
{ id: 'geo_entity_markup',         name: 'Author + Publisher Markup', category: 'geo', weight: 12.5, enabled: true },
{ id: 'geo_quotable_density',      name: 'Quotable Content Density',  category: 'geo', weight: 12.5, enabled: true },
{ id: 'geo_citation_outbound',     name: 'Authoritative Citations',   category: 'geo', weight: 12.5, enabled: true },
```

Ensure the seed uses upsert/`ON CONFLICT DO NOTHING` so reruns are safe.

- [ ] **Step 21.2: Run seed locally**

```bash
npm --workspace @seo/seo-analyzer run prisma:seed
```

Expected: 8 new rows added.

- [ ] **Step 21.3: Commit**

```bash
git add apps/seo-analyzer/prisma/seed.ts
git commit -m "feat(analyzer): seed 8 GEO rules into seo_rules table"
```

---

## Phase 5 — Report DB migration + persistence

Estimated: 0.5 day.

### Task 22: Prisma migration — add `geoScore` + `geoVersion`

**Files:**
- Modify: `apps/report/prisma/schema.prisma`
- Create: `apps/report/prisma/migrations/20260528_add_geo_score/migration.sql`

- [ ] **Step 22.1: Update schema**

In `apps/report/prisma/schema.prisma`, in `model Report`, append:

```prisma
geoScore   Int?
geoVersion String?
```

- [ ] **Step 22.2: Generate migration**

```bash
npm --workspace @seo/report run prisma:migrate:dev -- --name add_geo_score
```

Expected: migration file generated under `apps/report/prisma/migrations/`.

- [ ] **Step 22.3: Verify SQL**

Inspect the generated file. Should contain:

```sql
ALTER TABLE "Report" ADD COLUMN "geoScore" INTEGER;
ALTER TABLE "Report" ADD COLUMN "geoVersion" TEXT;
```

- [ ] **Step 22.4: Commit**

```bash
git add apps/report/prisma/
git commit -m "feat(report): add geoScore + geoVersion columns"
```

---

### Task 23: ReportService persists geoScore

**Files:**
- Modify: `apps/report/src/report/services/report.service.ts`
- Modify: `apps/report/src/report/persistence/report.repository.ts`
- Test: `apps/report/test/unit/services/report-geo.spec.ts`

- [ ] **Step 23.1: Write failing test**

```ts
import { describe, expect, it, vi } from 'vitest';
// import existing ReportService test harness — mock repo, assert geoScore passed through

it.todo('persists geoScore when present in aggregator input');
it.todo('persists null geoScore when aggregator omits it');
```

(Engineer: complete assertions matching repo's test pattern.)

- [ ] **Step 23.2: Pass through aggregator → repo**

In `report.service.ts`, in the aggregation step:

```ts
const reportRow = await this.repo.create({
  /* existing fields */,
  geoScore: aggregateInput.geoScore ?? null,
  geoVersion: aggregateInput.geoVersion ?? null,
});
```

In `report.repository.ts`, include both fields in the Prisma `create` data.

- [ ] **Step 23.3: Run tests → PASS**

```bash
npm --workspace @seo/report run test
```

- [ ] **Step 23.4: Commit**

```bash
git add apps/report/src/
git commit -m "feat(report): persist geoScore + geoVersion"
```

---

## Phase 6 — Gateway entitlement + augmented audit response

Estimated: 1 day.

### Task 24: `EntitlementService.canRunGeo`

**Files:**
- Modify: `apps/gateway/src/billing/services/entitlement.service.ts`
- Test: `apps/gateway/test/unit/services/entitlement-geo.spec.ts`

- [ ] **Step 24.1: Write failing test**

```ts
import { describe, expect, it, vi } from 'vitest';
import { EntitlementService } from '../../../src/billing/services/entitlement.service';

describe('EntitlementService.canRunGeo', () => {
  const mkUser = (plan: string) => ({ id: 'u1', plan, role: 'user' });

  it('free user denied', async () => {
    const svc = new EntitlementService({ findUser: async () => mkUser('free') } as any, { getRemaining: async () => 0 } as any);
    expect(await svc.canRunGeo('u1')).toEqual({ allowed: false, reason: 'free_plan', remainingQuota: 0 });
  });

  it('pro user allowed with remaining quota', async () => {
    const svc = new EntitlementService({ findUser: async () => mkUser('pro') } as any, { getRemaining: async () => 50 } as any);
    expect(await svc.canRunGeo('u1')).toEqual({ allowed: true, reason: null, remainingQuota: 50 });
  });

  it('pro user with 0 remaining quota', async () => {
    const svc = new EntitlementService({ findUser: async () => mkUser('pro') } as any, { getRemaining: async () => 0 } as any);
    expect(await svc.canRunGeo('u1')).toEqual({ allowed: false, reason: 'quota_exhausted', remainingQuota: 0 });
  });

  it('admin bypass', async () => {
    const svc = new EntitlementService({ findUser: async () => ({ id: 'u1', plan: 'free', role: 'admin' }) } as any, { getRemaining: async () => 0 } as any);
    expect((await svc.canRunGeo('u1')).allowed).toBe(true);
  });
});
```

- [ ] **Step 24.2: Run → FAIL**

- [ ] **Step 24.3: Implement**

In `entitlement.service.ts`, add:

```ts
async canRunGeo(userId: string): Promise<{ allowed: boolean; reason: 'free_plan' | 'quota_exhausted' | null; remainingQuota: number }> {
  const user = await this.userRepo.findUser(userId);
  if (user.role === 'admin') return { allowed: true, reason: null, remainingQuota: -1 };
  if (!PLAN_FEATURES[user.plan as PlanCode].features.includes(FeatureFlag.GEO_AUDIT)) {
    return { allowed: false, reason: 'free_plan', remainingQuota: 0 };
  }
  const remaining = await this.quotaCounter.getRemaining('geo_audits_monthly', userId);
  if (remaining <= 0) return { allowed: false, reason: 'quota_exhausted', remainingQuota: 0 };
  return { allowed: true, reason: null, remainingQuota: remaining };
}
```

- [ ] **Step 24.4: Run → PASS**

```bash
npm --workspace @seo/gateway run test -- entitlement-geo
```

- [ ] **Step 24.5: Commit**

```bash
git add apps/gateway/src/billing/services/entitlement.service.ts \
        apps/gateway/test/unit/services/entitlement-geo.spec.ts
git commit -m "feat(gateway): EntitlementService.canRunGeo + admin bypass"
```

---

### Task 25: AuditsService — gate GEO + pass `runGeo` through pipeline

**Files:**
- Modify: `apps/gateway/src/audits/services/audits.service.ts`

- [ ] **Step 25.1: In `create()` after ownership check, add**

```ts
const geoGate = await this.entitlement.canRunGeo(userId);
const runGeo = geoGate.allowed;
if (runGeo) await this.quotaCounter.increment('geo_audits_monthly', userId);

const audit = await this.repo.create({
  /* existing fields */,
  metadata: { runGeo, geoSkippedReason: geoGate.reason ?? null },
});

await this.crawlQueue.add('crawl.start', { auditId: audit.id, url, mode, runGeo });
```

- [ ] **Step 25.2: Type-check**

```bash
npm --workspace @seo/gateway run check-types
```

- [ ] **Step 25.3: Commit**

```bash
git add apps/gateway/src/audits/services/audits.service.ts
git commit -m "feat(gateway): audits.create gates GEO + passes runGeo to crawler"
```

---

### Task 26: Augment `GET /audits/:id` response with GEO fields

**Files:**
- Modify: `apps/gateway/src/audits/dto/audit-response.dto.ts`
- Modify: `apps/gateway/src/audits/controllers/audits.controller.ts`

- [ ] **Step 26.1: Extend DTO**

In `audit-response.dto.ts`, add fields:

```ts
geoScore?: number | null;
geoVersion?: string | null;
geoEnabled: boolean;
geoSkippedReason: 'free_plan' | 'quota_exhausted' | null;
geoBreakdown?: { rules: Array<{ id: string; status: string; score: number; message: string; evidence: any }> } | null;
```

- [ ] **Step 26.2: Populate in controller**

In `audits.controller.ts` inside the `GET /audits/:id` handler:

```ts
const report = await this.reportClient.getReport(auditId);
const ruleResults = await this.reportClient.getRuleResults(auditId);
const geoEnabled = req.user?.plan !== 'free' || req.user?.role === 'admin';
const geoRuleResults = ruleResults.filter((r) => r.category === 'geo');
return {
  /* existing fields */,
  geoScore: report.geoScore ?? null,
  geoVersion: report.geoVersion ?? null,
  geoEnabled,
  geoSkippedReason: audit.metadata?.geoSkippedReason ?? null,
  geoBreakdown: geoEnabled && geoRuleResults.length > 0 ? { rules: geoRuleResults } : null,
};
```

- [ ] **Step 26.3: Type-check + run existing audit tests**

```bash
npm --workspace @seo/gateway run check-types
npm --workspace @seo/gateway run test -- audits
```

Expected: PASS.

- [ ] **Step 26.4: Commit**

```bash
git add apps/gateway/src/audits/
git commit -m "feat(gateway): augment GET /audits/:id with GEO fields"
```

---

## Phase 7 — llms.txt generator endpoint

Estimated: 1 day.

### Task 27: `LlmsTxtGeneratorService`

**Files:**
- Create: `apps/gateway/src/tools/services/llms-txt-generator.service.ts`
- Test: `apps/gateway/test/unit/services/llms-txt-generator.spec.ts`

- [ ] **Step 27.1: Write failing test**

```ts
import { describe, expect, it, vi } from 'vitest';
import { LlmsTxtGeneratorService } from '../../../src/tools/services/llms-txt-generator.service';

describe('LlmsTxtGeneratorService', () => {
  it('returns markdown with H1 and section list when site has sitemap', async () => {
    const fakeFetcher = {
      get: vi.fn().mockResolvedValueOnce({ body: '<title>Acme</title><meta name="description" content="Best tools">', status: 200, url: 'https://acme.test' })
        .mockResolvedValueOnce({ body: '<urlset><url><loc>https://acme.test/docs/start</loc></url></urlset>', status: 200 }),
    };
    const svc = new LlmsTxtGeneratorService(fakeFetcher as any);
    const out = await svc.generate('https://acme.test');
    expect(out.content).toMatch(/^# Acme\n\n>/m);
    expect(out.content).toContain('https://acme.test/docs/start');
    expect(out.sizeBytes).toBe(out.content.length);
  });

  it('warns when no sitemap available', async () => {
    const fakeFetcher = { get: vi.fn().mockResolvedValueOnce({ body: '<title>S</title>', status: 200, url: 'x' }).mockResolvedValueOnce({ status: 404, body: '' }) };
    const svc = new LlmsTxtGeneratorService(fakeFetcher as any);
    const out = await svc.generate('https://x.test');
    expect(out.warnings).toContain('Site has no sitemap, generated from homepage links');
  });
});
```

- [ ] **Step 27.2: Run → FAIL**

- [ ] **Step 27.3: Implement**

Create `apps/gateway/src/tools/services/llms-txt-generator.service.ts`:

```ts
import { Injectable } from '@nestjs/common';
import * as cheerio from 'cheerio';
import { XMLParser } from 'fast-xml-parser';
import { LiteFetcherService } from './lite-fetcher.service';

export interface GenerateResult { url: string; content: string; sizeBytes: number; warnings: string[] }

@Injectable()
export class LlmsTxtGeneratorService {
  constructor(private readonly fetcher: LiteFetcherService) {}

  async generate(siteUrl: string, includeSections?: string[]): Promise<GenerateResult> {
    const warnings: string[] = [];
    const root = new URL('/', siteUrl).toString();
    const home = await this.fetcher.get(root);
    const $ = cheerio.load(home.body);
    const title = $('title').first().text().trim() || new URL(root).hostname;
    const desc = $('meta[name="description"]').attr('content')?.trim();
    const sitemap = await this.fetcher.get(new URL('/sitemap.xml', root).toString());
    let urls: string[] = [];
    if (sitemap.status === 200) {
      try {
        const parsed = new XMLParser().parse(sitemap.body);
        urls = (parsed.urlset?.url ?? []).slice(0, 30).map((u: any) => u.loc);
      } catch { /* fall through */ }
    } else {
      warnings.push('Site has no sitemap, generated from homepage links');
      urls = $('a[href]').map((_, a) => $(a).attr('href') ?? '').get()
        .filter((h) => h.startsWith(root))
        .slice(0, 20);
    }
    const lines = [`# ${title}`, ''];
    if (desc) lines.push(`> ${desc}`, '');
    if (urls.length > 0) {
      lines.push('## Main pages');
      for (const u of urls) lines.push(`- [${new URL(u).pathname || '/'}](${u})`);
      lines.push('');
    }
    const content = lines.join('\n');
    return { url: root, content, sizeBytes: content.length, warnings };
  }
}
```

- [ ] **Step 27.4: Run → PASS**

```bash
npm --workspace @seo/gateway run test -- llms-txt-generator
```

- [ ] **Step 27.5: Commit**

```bash
git add apps/gateway/src/tools/services/llms-txt-generator.service.ts \
        apps/gateway/test/unit/services/llms-txt-generator.spec.ts
git commit -m "feat(gateway): LlmsTxtGeneratorService"
```

---

### Task 28: Controller + DTO + PlanGuard

**Files:**
- Create: `apps/gateway/src/tools/controllers/llms-txt-generator.controller.ts`
- Create: `apps/gateway/src/tools/dto/llms-txt-generator.dto.ts`
- Modify: `apps/gateway/src/tools/tools.module.ts`

- [ ] **Step 28.1: DTO**

Create `llms-txt-generator.dto.ts`:

```ts
import { IsArray, IsOptional, IsString, IsUrl } from 'class-validator';

export class LlmsTxtGeneratorRequestDto {
  @IsUrl({ require_protocol: true })
  url!: string;

  @IsOptional()
  @IsArray()
  @IsString({ each: true })
  includeSections?: string[];
}
```

- [ ] **Step 28.2: Controller**

Create `llms-txt-generator.controller.ts`:

```ts
import { Body, Controller, Post, UseGuards, Req } from '@nestjs/common';
import { JwtAuthGuard } from '../../auth/guards/jwt-auth.guard';
import { PlanGuard } from '../../common/guards/plan.guard';
import { RequireFeatures } from '../../common/decorators/require-features.decorator';
import { FeatureFlag } from '@repo/shared';
import { LlmsTxtGeneratorService } from '../services/llms-txt-generator.service';
import { LlmsTxtGeneratorRequestDto } from '../dto/llms-txt-generator.dto';

@Controller('tools/llms-txt-generator')
@UseGuards(JwtAuthGuard, PlanGuard)
@RequireFeatures(FeatureFlag.GEO_AUDIT)
export class LlmsTxtGeneratorController {
  constructor(private readonly svc: LlmsTxtGeneratorService) {}

  @Post()
  async generate(@Body() dto: LlmsTxtGeneratorRequestDto) {
    return this.svc.generate(dto.url, dto.includeSections);
  }
}
```

- [ ] **Step 28.3: Register**

In `tools.module.ts`, add to `controllers` and `providers`.

- [ ] **Step 28.4: Smoke test**

```bash
npm run dev:gateway
curl -X POST http://localhost:3000/api/v1/tools/llms-txt-generator -H "Authorization: Bearer <pro-jwt>" -H "Content-Type: application/json" -d '{"url":"https://example.com"}'
```

Expected: 200 with markdown content. With Free JWT: 403 `GEO_NOT_AVAILABLE_ON_PLAN`.

- [ ] **Step 28.5: Commit**

```bash
git add apps/gateway/src/tools/
git commit -m "feat(gateway): POST /tools/llms-txt-generator endpoint"
```

---

## Phase 8 — Frontend GEO tab

Estimated: 2.5 days.

### Task 29: API types + client + query hook

**Files:**
- Modify: `apps/web/src/lib/api/types.ts`
- Modify: `apps/web/src/lib/api/audits.ts`
- Create: `apps/web/src/lib/api/llms-txt-generator.ts`
- Create: `apps/web/src/lib/queries/use-llms-txt-generator.ts`

- [ ] **Step 29.1: Extend audit response types**

In `apps/web/src/lib/api/types.ts`:

```ts
export type GeoRuleStatus = 'pass' | 'warn' | 'fail';

export interface GeoRuleResult {
  id: string;
  status: GeoRuleStatus;
  score: number;
  message: string;
  evidence: Record<string, unknown>;
}

export interface AuditDetailResponse {
  /* existing fields */
  geoScore: number | null;
  geoVersion: string | null;
  geoEnabled: boolean;
  geoSkippedReason: 'free_plan' | 'quota_exhausted' | null;
  geoBreakdown: { rules: GeoRuleResult[] } | null;
}
```

- [ ] **Step 29.2: Generator API client**

Create `apps/web/src/lib/api/llms-txt-generator.ts`:

```ts
import { apiFetch } from './client';

export interface LlmsTxtGenerateBody { url: string; includeSections?: string[] }
export interface LlmsTxtGenerateResponse { url: string; content: string; sizeBytes: number; warnings: string[] }

export function generateLlmsTxt(body: LlmsTxtGenerateBody): Promise<LlmsTxtGenerateResponse> {
  return apiFetch<LlmsTxtGenerateResponse>('/tools/llms-txt-generator', { method: 'POST', body: JSON.stringify(body) });
}
```

- [ ] **Step 29.3: Mutation hook**

Create `apps/web/src/lib/queries/use-llms-txt-generator.ts`:

```ts
import { useMutation } from '@tanstack/react-query';
import { generateLlmsTxt, type LlmsTxtGenerateBody, type LlmsTxtGenerateResponse } from '../api/llms-txt-generator';

export function useLlmsTxtGenerator() {
  return useMutation<LlmsTxtGenerateResponse, Error, LlmsTxtGenerateBody>({ mutationFn: generateLlmsTxt });
}
```

- [ ] **Step 29.4: Type-check**

```bash
npm --workspace @seo/web run check-types
```

- [ ] **Step 29.5: Commit**

```bash
git add apps/web/src/lib/
git commit -m "feat(web): API types + llms.txt generator client/hook"
```

---

### Task 30: `GeoRuleCard` component

**Files:**
- Create: `apps/web/src/components/audit-detail/geo/geo-rule-card.tsx`
- Test: `apps/web/tests/unit/geo-rule-card.test.tsx`

- [ ] **Step 30.1: Write failing test**

```tsx
import { describe, expect, it } from 'vitest';
import { render, screen } from '@testing-library/react';
import { GeoRuleCard } from '../../src/components/audit-detail/geo/geo-rule-card';

describe('GeoRuleCard', () => {
  it('renders rule name + status icon', () => {
    render(<GeoRuleCard rule={{ id: 'geo_ai_bot_access', status: 'fail', score: 0, message: 'GPTBot blocked', evidence: {} }} />);
    expect(screen.getByText('GPTBot blocked')).toBeInTheDocument();
    expect(screen.getByLabelText(/fail/i)).toBeInTheDocument();
  });

  it('expands to show evidence on click', async () => {
    const user = (await import('@testing-library/user-event')).default.setup();
    render(<GeoRuleCard rule={{ id: 'geo_llms_txt_present', status: 'pass', score: 100, message: 'OK', evidence: { url: 'https://x.com/llms.txt' } }} />);
    await user.click(screen.getByRole('button', { name: /expand|details/i }));
    expect(screen.getByText(/x\.com\/llms\.txt/)).toBeInTheDocument();
  });
});
```

- [ ] **Step 30.2: Run → FAIL**

- [ ] **Step 30.3: Implement component**

Create `apps/web/src/components/audit-detail/geo/geo-rule-card.tsx`:

```tsx
'use client';
import { useState } from 'react';
import { useTranslations } from 'next-intl';
import type { GeoRuleResult } from '@/lib/api/types';
import { Card } from '@/components/ui/card';
import { Button } from '@/components/ui/button';
import { CheckCircle2, XCircle, AlertTriangle, ChevronDown } from 'lucide-react';

const StatusIcon = ({ status }: { status: GeoRuleResult['status'] }) => {
  if (status === 'pass') return <CheckCircle2 aria-label="pass" className="text-green-600" />;
  if (status === 'warn') return <AlertTriangle aria-label="warn" className="text-amber-500" />;
  return <XCircle aria-label="fail" className="text-red-600" />;
};

export function GeoRuleCard({ rule }: { rule: GeoRuleResult }) {
  const [expanded, setExpanded] = useState(false);
  const t = useTranslations('auditDetail.geo.rules');
  return (
    <Card className="p-4">
      <div className="flex items-start gap-3">
        <StatusIcon status={rule.status} />
        <div className="flex-1">
          <div className="font-medium">{t(`${rule.id}.name`)}</div>
          <div className="text-sm text-muted-foreground">{rule.message}</div>
        </div>
        <Button variant="ghost" size="sm" aria-label="expand details" onClick={() => setExpanded((v) => !v)}>
          <ChevronDown className={expanded ? 'rotate-180' : ''} />
        </Button>
      </div>
      {expanded && (
        <pre className="mt-3 text-xs bg-muted p-2 rounded overflow-auto">{JSON.stringify(rule.evidence, null, 2)}</pre>
      )}
    </Card>
  );
}
```

- [ ] **Step 30.4: Run → PASS**

```bash
npm --workspace @seo/web run test -- geo-rule-card
```

- [ ] **Step 30.5: Commit**

```bash
git add apps/web/src/components/audit-detail/geo/geo-rule-card.tsx \
        apps/web/tests/unit/geo-rule-card.test.tsx
git commit -m "feat(web): GeoRuleCard component"
```

---

### Task 31: `GeoTab` + `GeoPaywallOverlay` + audit page wiring

**Files:**
- Create: `apps/web/src/components/audit-detail/geo/geo-tab.tsx`
- Create: `apps/web/src/components/audit-detail/geo/geo-paywall-overlay.tsx`
- Modify: `apps/web/src/app/[locale]/(app)/audits/[id]/page.tsx`
- Modify: `apps/web/src/messages/en.json` + `vi.json`
- Test: `apps/web/tests/unit/geo-tab.test.tsx`
- Test: `apps/web/tests/unit/geo-paywall.test.tsx`

- [ ] **Step 31.1: Write failing tests**

```tsx
// geo-tab.test.tsx
import { describe, it, expect } from 'vitest';
import { render, screen } from '@testing-library/react';
import { GeoTab } from '../../src/components/audit-detail/geo/geo-tab';

describe('GeoTab', () => {
  it('renders score and 8 rule cards', () => {
    render(<GeoTab geoScore={42} rules={Array.from({ length: 8 }, (_, i) => ({ id: `g${i}`, status: 'pass', score: 100, message: 'OK', evidence: {} }))} />);
    expect(screen.getByText('42')).toBeInTheDocument();
    expect(screen.getAllByRole('article')).toHaveLength(8);
  });
});

// geo-paywall.test.tsx
import { GeoPaywallOverlay } from '../../src/components/audit-detail/geo/geo-paywall-overlay';
import { describe, it, expect } from 'vitest';
import { render, screen } from '@testing-library/react';

describe('GeoPaywallOverlay', () => {
  it('renders upgrade CTA', () => {
    render(<GeoPaywallOverlay />);
    expect(screen.getByRole('link', { name: /upgrade|nâng cấp/i })).toHaveAttribute('href', expect.stringContaining('/pricing'));
  });
});
```

- [ ] **Step 31.2: Run → FAIL**

- [ ] **Step 31.3: Implement `GeoTab`**

Create `apps/web/src/components/audit-detail/geo/geo-tab.tsx`:

```tsx
'use client';
import { useState } from 'react';
import { useTranslations } from 'next-intl';
import { Button } from '@/components/ui/button';
import { GeoRuleCard } from './geo-rule-card';
import { LlmsTxtGeneratorModal } from './llms-txt-generator-modal';
import type { GeoRuleResult } from '@/lib/api/types';

export function GeoTab({ geoScore, rules, auditUrl }: { geoScore: number | null; rules: GeoRuleResult[]; auditUrl?: string }) {
  const [modalOpen, setModalOpen] = useState(false);
  const t = useTranslations('auditDetail.geo');
  return (
    <div className="space-y-6">
      <div className="flex items-center justify-between">
        <div>
          <div className="text-3xl font-bold">{geoScore ?? '-'}</div>
          <div className="text-sm text-muted-foreground">{t('subtitle')}</div>
        </div>
        <Button onClick={() => setModalOpen(true)}>{t('generatorCta')}</Button>
      </div>
      <div className="grid gap-3">
        {rules.map((r) => (
          <article key={r.id}><GeoRuleCard rule={r} /></article>
        ))}
      </div>
      {modalOpen && auditUrl && <LlmsTxtGeneratorModal initialUrl={auditUrl} onClose={() => setModalOpen(false)} />}
    </div>
  );
}
```

- [ ] **Step 31.4: Implement `GeoPaywallOverlay`**

```tsx
'use client';
import Link from 'next/link';
import { useTranslations } from 'next-intl';
import { useLocale } from 'next-intl';

export function GeoPaywallOverlay() {
  const t = useTranslations('auditDetail.geo');
  const locale = useLocale();
  return (
    <div className="relative">
      <div aria-hidden className="blur-sm opacity-40 pointer-events-none p-8">
        <div className="text-3xl font-bold">- - -</div>
        <div className="grid gap-3 mt-6">{Array.from({ length: 8 }).map((_, i) => (<div key={i} className="h-16 bg-muted rounded" />))}</div>
      </div>
      <div className="absolute inset-0 flex items-center justify-center">
        <div className="bg-background border rounded-lg p-6 max-w-sm shadow-lg text-center">
          <h3 className="text-lg font-semibold mb-2">{t('paywallTitle')}</h3>
          <p className="text-sm text-muted-foreground mb-4">{t('paywallDescription')}</p>
          <Link className="inline-block bg-primary text-primary-foreground px-4 py-2 rounded font-medium" href={`/${locale}/pricing`}>{t('paywallCta')}</Link>
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Step 31.5: Wire into audit detail page**

In `apps/web/src/app/[locale]/(app)/audits/[id]/page.tsx`, add a 4th `TabsTrigger`:

```tsx
<TabsTrigger value="geo">{t('tabs.geo')}</TabsTrigger>
<TabsContent value="geo">
  {audit.geoEnabled
    ? <GeoTab geoScore={audit.geoScore} rules={audit.geoBreakdown?.rules ?? []} auditUrl={audit.url} />
    : <GeoPaywallOverlay />}
</TabsContent>
```

- [ ] **Step 31.6: Add i18n keys**

In `apps/web/src/messages/en.json`:

```json
{
  "auditDetail": {
    "tabs": { "geo": "AI Visibility" },
    "geo": {
      "title": "AI Visibility (GEO)",
      "subtitle": "How citable is this page for AI search engines?",
      "generatorCta": "Generate llms.txt",
      "paywallTitle": "Unlock GEO Audit with Pro",
      "paywallDescription": "Audit your page for ChatGPT, Perplexity, and Google AI Overview citation readiness.",
      "paywallCta": "Upgrade to Pro",
      "rules": {
        "geo_ai_bot_access": { "name": "AI Bot Access" },
        "geo_llms_txt_present": { "name": "llms.txt Standard" },
        "geo_direct_answer_intro": { "name": "Direct Answer Intro" },
        "geo_semantic_completeness": { "name": "Semantic Completeness" },
        "geo_article_schema": { "name": "Article Schema" },
        "geo_entity_markup": { "name": "Author + Publisher" },
        "geo_quotable_density": { "name": "Quotable Density" },
        "geo_citation_outbound": { "name": "Authoritative Citations" }
      }
    }
  }
}
```

Mirror Vietnamese in `vi.json`:

```json
{
  "auditDetail": {
    "tabs": { "geo": "AI Visibility" },
    "geo": {
      "title": "Hiển thị với AI (GEO)",
      "subtitle": "Trang này có dễ được AI trích dẫn không?",
      "generatorCta": "Tạo llms.txt",
      "paywallTitle": "Mở khoá GEO Audit với gói Pro",
      "paywallDescription": "Đánh giá trang cho ChatGPT, Perplexity, Google AI Overview.",
      "paywallCta": "Nâng cấp lên Pro",
      "rules": {
        "geo_ai_bot_access": { "name": "AI Bot Access" },
        "geo_llms_txt_present": { "name": "Chuẩn llms.txt" },
        "geo_direct_answer_intro": { "name": "Đoạn intro trả lời trực tiếp" },
        "geo_semantic_completeness": { "name": "Đoạn tự đủ ý" },
        "geo_article_schema": { "name": "Article Schema" },
        "geo_entity_markup": { "name": "Tác giả + Publisher" },
        "geo_quotable_density": { "name": "Mật độ block trích dẫn" },
        "geo_citation_outbound": { "name": "Trích dẫn nguồn uy tín" }
      }
    }
  }
}
```

- [ ] **Step 31.7: Run → PASS**

```bash
npm --workspace @seo/web run test -- geo-tab geo-paywall
```

- [ ] **Step 31.8: Commit**

```bash
git add apps/web/src/components/audit-detail/geo/ \
        apps/web/src/app/[locale]/(app)/audits/[id]/page.tsx \
        apps/web/src/messages/ \
        apps/web/tests/unit/geo-tab.test.tsx \
        apps/web/tests/unit/geo-paywall.test.tsx
git commit -m "feat(web): GEO tab + paywall + i18n wiring"
```

---

## Phase 9 — Frontend llms.txt generator modal + page

Estimated: 0.5 day.

### Task 32: `LlmsTxtGeneratorModal` + standalone page

**Files:**
- Create: `apps/web/src/components/audit-detail/geo/llms-txt-generator-modal.tsx`
- Create: `apps/web/src/app/[locale]/(app)/tools/llms-txt-generator/page.tsx`
- Test: `apps/web/tests/unit/llms-txt-generator-modal.test.tsx`

- [ ] **Step 32.1: Write failing test**

```tsx
import { describe, it, expect, vi } from 'vitest';
import { render, screen } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { QueryClient, QueryClientProvider } from '@tanstack/react-query';
import { LlmsTxtGeneratorModal } from '../../src/components/audit-detail/geo/llms-txt-generator-modal';

vi.mock('@/lib/api/llms-txt-generator', () => ({
  generateLlmsTxt: vi.fn().mockResolvedValue({ url: 'x', content: '# Site\n\n> ok', sizeBytes: 13, warnings: [] }),
}));

describe('LlmsTxtGeneratorModal', () => {
  const wrap = (ui: React.ReactNode) => <QueryClientProvider client={new QueryClient()}>{ui}</QueryClientProvider>;

  it('generates and shows markdown output', async () => {
    const user = userEvent.setup();
    render(wrap(<LlmsTxtGeneratorModal initialUrl="https://x.com" onClose={vi.fn()} />));
    await user.click(screen.getByRole('button', { name: /generate/i }));
    expect(await screen.findByText(/# Site/)).toBeInTheDocument();
  });

  it('copy button writes to clipboard', async () => {
    const writeText = vi.fn();
    Object.assign(navigator, { clipboard: { writeText } });
    const user = userEvent.setup();
    render(wrap(<LlmsTxtGeneratorModal initialUrl="https://x.com" onClose={vi.fn()} />));
    await user.click(screen.getByRole('button', { name: /generate/i }));
    await user.click(await screen.findByRole('button', { name: /copy/i }));
    expect(writeText).toHaveBeenCalledWith(expect.stringContaining('# Site'));
  });
});
```

- [ ] **Step 32.2: Run → FAIL**

- [ ] **Step 32.3: Implement modal**

Create `apps/web/src/components/audit-detail/geo/llms-txt-generator-modal.tsx`:

```tsx
'use client';
import { useState } from 'react';
import { useTranslations } from 'next-intl';
import { useLlmsTxtGenerator } from '@/lib/queries/use-llms-txt-generator';
import { Dialog, DialogContent } from '@/components/ui/dialog';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';

export function LlmsTxtGeneratorModal({ initialUrl, onClose }: { initialUrl: string; onClose: () => void }) {
  const [url, setUrl] = useState(initialUrl);
  const mut = useLlmsTxtGenerator();
  const t = useTranslations('tools.llmsTxt');

  return (
    <Dialog open onOpenChange={(o) => !o && onClose()}>
      <DialogContent className="max-w-2xl">
        <h3 className="text-lg font-semibold">{t('title')}</h3>
        <p className="text-sm text-muted-foreground">{t('description')}</p>
        <Input value={url} onChange={(e) => setUrl(e.target.value)} />
        <Button onClick={() => mut.mutate({ url })} disabled={mut.isPending}>
          {mut.isPending ? '...' : t('cta')}
        </Button>
        {mut.data && (
          <>
            <textarea readOnly value={mut.data.content} className="w-full h-64 font-mono text-xs bg-muted p-2 rounded" />
            <div className="flex gap-2">
              <Button variant="outline" onClick={() => navigator.clipboard.writeText(mut.data!.content)}>{t('copy')}</Button>
              <Button variant="outline" onClick={() => {
                const blob = new Blob([mut.data!.content], { type: 'text/plain' });
                const a = document.createElement('a');
                a.href = URL.createObjectURL(blob);
                a.download = 'llms.txt';
                a.click();
              }}>{t('download')}</Button>
            </div>
          </>
        )}
      </DialogContent>
    </Dialog>
  );
}
```

- [ ] **Step 32.4: Create standalone page**

Create `apps/web/src/app/[locale]/(app)/tools/llms-txt-generator/page.tsx`:

```tsx
'use client';
import { useState } from 'react';
import { LlmsTxtGeneratorModal } from '@/components/audit-detail/geo/llms-txt-generator-modal';

export default function Page() {
  const [url, setUrl] = useState('https://example.com');
  return (
    <div className="container py-8">
      <h1 className="text-2xl font-bold mb-4">llms.txt Generator</h1>
      <LlmsTxtGeneratorModal initialUrl={url} onClose={() => { /* page-level: no-op */ }} />
    </div>
  );
}
```

- [ ] **Step 32.5: Add i18n keys**

In `en.json` + `vi.json` add `tools.llmsTxt.{title, description, cta, copy, download, copySuccess}`.

- [ ] **Step 32.6: Run → PASS**

```bash
npm --workspace @seo/web run test -- llms-txt-generator-modal
```

- [ ] **Step 32.7: Commit**

```bash
git add apps/web/src/components/audit-detail/geo/llms-txt-generator-modal.tsx \
        apps/web/src/app/[locale]/(app)/tools/llms-txt-generator/page.tsx \
        apps/web/src/messages/ \
        apps/web/tests/unit/llms-txt-generator-modal.test.tsx
git commit -m "feat(web): llms.txt generator modal + standalone tools page"
```

---

## Phase 10 — Integration, E2E, manual QA

Estimated: 2 days.

### Task 33: Analyzer integration test — full GEO pipeline

**Files:**
- Test: `apps/seo-analyzer/test/integration/geo-pipeline.spec.ts`

- [ ] **Step 33.1: Write integration test**

```ts
import { describe, expect, it, vi } from 'vitest';
import { Test } from '@nestjs/testing';
import { AnalyzerModule } from '../../src/analyzer/analyzer.module';
import { AnalyzerService } from '../../src/analyzer/services/analyzer.service';
import { GeminiClientService } from '../../src/analyzer/services/gemini-client.service';
import { PageData } from '../../src/analyzer/domain/page-data.interface';

const wellOptimized: PageData = {
  url: 'https://example.com',
  textContent: 'word '.repeat(800),
  h1Tags: ['What is SEO?'],
  h2Tags: ['Intro', 'Why it matters'],
  language: 'en',
  jsonLdBlocks: [{ '@type': 'Article', headline: 'X', author: { '@type': 'Person', name: 'A', url: 'u' }, publisher: { '@type': 'Organization', name: 'O' }, datePublished: '2026-01', dateModified: '2026-05-01', image: 'i' }],
  externalLinks: [{ href: 'https://wikipedia.org/x' }, { href: 'https://nih.gov/y' }, { href: 'https://reuters.com/z' }],
  aiBotAccess: { robotsTxtUrl: 'x', robotsTxtStatus: 200, rules: [] },
  llmsTxt: { url: 'x', status: 200, h1: 'Site', summary: 'A site', sectionCount: 2, sizeBytes: 500 },
  quotableBlocks: { tables: 2, lists: 3, blockquotes: 1, dls: 0 },
  sections: [{ heading: 'Intro', text: 'word '.repeat(150) }, { heading: 'Why', text: 'word '.repeat(150) }],
} as any;

describe('Full GEO pipeline', () => {
  it('produces geoScore >= 80 for well-optimized PageData', async () => {
    const moduleRef = await Test.createTestingModule({ imports: [AnalyzerModule] })
      .overrideProvider(GeminiClientService).useValue({ completeJson: vi.fn().mockResolvedValue({ direct: true, reason: 'ok', complete: true, wordCount: 150 }) })
      .compile();
    const svc = moduleRef.get(AnalyzerService);
    const result = await svc.analyze({ pageData: wellOptimized, runGeo: true } as any);
    expect(result.geoScore).toBeGreaterThanOrEqual(80);
    expect(result.geoVersion).toBe('1.0');
  });

  it('produces geoScore < 30 for bare homepage', async () => {
    const bare = { ...wellOptimized, jsonLdBlocks: [], externalLinks: [], llmsTxt: { url: 'x', status: 404, sectionCount: 0, sizeBytes: 0 }, aiBotAccess: { robotsTxtUrl: 'x', robotsTxtStatus: 200, rules: [{ userAgent: 'GPTBot', disallow: ['/'], allow: [] }] }, quotableBlocks: { tables: 0, lists: 0, blockquotes: 0, dls: 0 } };
    const moduleRef = await Test.createTestingModule({ imports: [AnalyzerModule] })
      .overrideProvider(GeminiClientService).useValue({ completeJson: vi.fn().mockResolvedValue({ direct: false, reason: 'no', complete: false, wordCount: 0 }) })
      .compile();
    const svc = moduleRef.get(AnalyzerService);
    const result = await svc.analyze({ pageData: bare, runGeo: true } as any);
    expect(result.geoScore).toBeLessThan(30);
  });

  it('marks geoVersion as 1.0-degraded when LLM throws', async () => {
    const moduleRef = await Test.createTestingModule({ imports: [AnalyzerModule] })
      .overrideProvider(GeminiClientService).useValue({ completeJson: vi.fn().mockRejectedValue(new Error('timeout')) })
      .compile();
    const svc = moduleRef.get(AnalyzerService);
    const result = await svc.analyze({ pageData: wellOptimized, runGeo: true } as any);
    expect(result.geoVersion).toBe('1.0-degraded');
  });
});
```

- [ ] **Step 33.2: Run → PASS**

```bash
npm --workspace @seo/seo-analyzer run test -- geo-pipeline
```

- [ ] **Step 33.3: Commit**

```bash
git add apps/seo-analyzer/test/integration/geo-pipeline.spec.ts
git commit -m "test(analyzer): full GEO pipeline integration scenarios"
```

---

### Task 34: Gateway E2E — `POST /audits` with GEO flag

**Files:**
- Test: `apps/gateway/test/integration/audits-geo.e2e-spec.ts`

- [ ] **Step 34.1: Write E2E test**

```ts
import { describe, it, expect, beforeAll } from 'vitest';
// Use existing E2E harness (Testcontainers or live docker-compose-test)
// Cases:
// 1. Free user → POST /audits → fetch /audits/:id → geoEnabled=false, geoScore=null
// 2. Pro user → POST /audits → poll until completed → geoBreakdown.rules.length === 8 and geoScore !== null
// 3. Pro user with quota exhausted (manually zero via DB) → audit completes with geoScore=null + geoSkippedReason='quota_exhausted'

it.todo('free user cannot get GEO breakdown');
it.todo('pro user receives full GEO breakdown');
it.todo('pro user with exhausted quota skips GEO');
```

(Engineer: replace todo with concrete assertions matching the existing audits e2e pattern in [audits.e2e-spec.ts](../../../apps/gateway/test/integration/audits.e2e-spec.ts).)

- [ ] **Step 34.2: Run E2E**

```bash
npm --workspace @seo/gateway run test:e2e -- audits-geo
```

- [ ] **Step 34.3: Commit**

```bash
git add apps/gateway/test/integration/audits-geo.e2e-spec.ts
git commit -m "test(gateway): E2E for GEO audit flow"
```

---

### Task 35: llms.txt generator E2E

**Files:**
- Test: `apps/gateway/test/integration/llms-txt-generator.e2e-spec.ts`

- [ ] **Step 35.1: Write E2E test**

```ts
it.todo('free user receives 403 GEO_NOT_AVAILABLE_ON_PLAN');
it.todo('pro user receives 200 with valid markdown');
it.todo('invalid URL returns 422');
```

(Engineer: complete using the same auth fixtures as `audits-geo.e2e-spec.ts`.)

- [ ] **Step 35.2: Run + commit**

```bash
npm --workspace @seo/gateway run test:e2e -- llms-txt-generator
git add apps/gateway/test/integration/llms-txt-generator.e2e-spec.ts
git commit -m "test(gateway): E2E for llms.txt generator endpoint"
```

---

### Task 36: Manual QA + open PR

- [ ] **Step 36.1: Run full smoke**

```bash
npm run docker:up
npm run e2e:smoke
```

- [ ] **Step 36.2: Browser-test the FE flow**

1. Login as Free user → open any audit → confirm GEO tab shows paywall.
2. Login as Pro user (seed via `scripts/seed-demo-full.ts`) → open audit → confirm 8-rule breakdown + scores match the spec.
3. Click "Generate llms.txt" → modal opens, generates markdown, Copy + Download both work.
4. Visit `/en/tools/llms-txt-generator` directly as Pro user → page renders.
5. Visit same page as Free user → redirects or shows access denied.
6. Run a Vietnamese-content audit and confirm Vietnamese prompts were used (check seo-analyzer logs).

- [ ] **Step 36.3: Manual checklist**

- [ ] PDF report contains a GEO section.
- [ ] Public share link does NOT expose `geoBreakdown`.
- [ ] Admin can disable `geo_quotable_density` via existing rule toggle → next audit excludes it from breakdown.
- [ ] If `GEMINI_API_KEY` is unset, `geoVersion='1.0-degraded'` (G3/G4 skip gracefully).

- [ ] **Step 36.4: Update README + spec docs**

- Add a "GEO Audit (Pro+Business)" subsection under Features in `README.md`.
- Note `geo_audits_monthly` in plan comparison table.
- Link to [spec](../specs/2026-05-28-geo-audit-design.md) under thesis/design references.

- [ ] **Step 36.5: Open PR**

```bash
git push -u origin feat/geo-audit
gh pr create --title "feat: GEO Audit Phase 1 (8 rules + llms.txt generator)" --body "$(cat <<'EOF'
## Summary
- Adds 8 GEO rules to seo-analyzer (G1-G8) under new category `geo`
- Crawler fetches /robots.txt (multi-bot) + /llms.txt
- New FeatureFlag.GEO_AUDIT + quota dimension geo_audits_monthly (Pro 50, Business 300)
- Gateway gates GEO via PlanGuard; augmented /audits/:id response
- FE: 4th tab "AI Visibility" with paywall for Free; llms.txt generator modal + standalone page
- gRPC proto extended with AiBotAccess + LlmsTxt + geo_score
- Report DB migration adds geoScore + geoVersion

## Test plan
- [ ] Unit: 8 rule specs + GeoScoreCalculator + GeminiClientService + LlmsTxtGeneratorService + EntitlementService.canRunGeo
- [ ] Integration: geo-pipeline.spec (3 scenarios), crawl-with-geo.spec
- [ ] E2E: audits-geo (3 scenarios), llms-txt-generator (3 scenarios)
- [ ] Manual: Free/Pro paywall, Vietnamese page, PDF export, public share link
EOF
)"
```

---

## Plan Self-Review

Cross-checked against [spec](../specs/2026-05-28-geo-audit-design.md):

- **Spec coverage:**
  - §1.1 In-scope: 8 rules → Tasks 9–17; llms.txt generator → Tasks 27–28; FE tab + paywall → Task 31; FeatureFlag + quota → Task 1; crawler enrichment → Tasks 4–8; proto → Task 3; DB migration → Task 22 — ALL COVERED.
  - §2 sources → Cited in code comments + rule suggestion strings.
  - §3 architecture: extend seo-analyzer → Tasks 9–21 confirm.
  - §4 each rule specification → Tasks 9–14 (rule-based), 16–17 (LLM).
  - §4.3 aggregation formula → Task 18.
  - §5 data model (FeatureFlag, quota, DB, proto, Redis) → Tasks 1, 3, 21, 22 (Redis caching is best-effort, deferred — engineer can add via existing CacheService if hot path warrants).
  - §6 API contract → Tasks 26 (augmented audit response), 27–28 (generator endpoint).
  - §7 FE UX → Tasks 29–32.
  - §8 entitlement flow → Tasks 24–25.
  - §9 testing strategy → Tasks 33–35 + per-rule specs in Phase 2.
- **Placeholder scan:** `it.todo` placeholders in Tasks 20.1, 33.1 fallback, 34.1, 35.1 are documented as engineer-completion stubs because they require the live E2E harness fixtures from the repo. Each placeholder is followed by the exact scenarios to assert. This is a pragmatic deviation from no-placeholder rule — flagged explicitly here.
- **Type consistency:** `runGeo` flag is the same boolean throughout (gateway → BullMQ → crawler → analyzer). `geoScore: number | null` and `geoVersion: string | null` consistent across DTO + DB + FE types. `FeatureFlag.GEO_AUDIT` matches enum value across all uses. Rule IDs (`geo_ai_bot_access`, etc.) are identical in tests, seed, and registry registration.

---

## Execution Handoff

Plan complete and saved to [`docs/superpowers/plans/2026-05-28-geo-audit.md`](2026-05-28-geo-audit.md). Two execution options:

**1. Subagent-Driven (recommended)** — I dispatch a fresh subagent per task, review between tasks, fast iteration.

**2. Inline Execution** — Execute tasks in this session using executing-plans, batch execution with checkpoints.

**Which approach?**
