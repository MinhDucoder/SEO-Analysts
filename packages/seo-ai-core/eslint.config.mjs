// Pass-through placeholder so `eslint .` doesn't fail during the K1 bootstrap
// commit when the package has no source files yet. Task K10 replaces this
// with the real adapter-boundary rule.
export default [];
